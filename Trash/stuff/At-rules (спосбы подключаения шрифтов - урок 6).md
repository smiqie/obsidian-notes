

- внутри директив (нпр @font-face) пишутся дискрипторы (нпр font-family: "MyFont"; - похожи на обычные ксс св-ва, но по факту это конфигурации ксс директивы)
# 1. `@font-face`

```css
/* подключение своего шрифта */
@font-face {
  font-family: "MyFont";          /* имя шрифта для CSS */
  src: url("myfont.woff2") format("woff2"),  /* путь к файлу + формат */
       url("myfont.woff") format("woff");    /* запасной вариант */
  font-weight: normal;            /* толщина: normal, bold, 100-900 */
  font-style: normal;             /* стиль: normal, italic, oblique */
  font-display: swap;             /* как отображать текст до загрузки шрифта */
}
```

1. **`font-family`** - имя шрифта (можем назвать как угодно - просто "ярлык". **как правило указывают имя подклбчаемого шрифта чтлобы не было путаницы1**)

   ```css
   body { font-family: "MyFont", sans-serif; }
   ```

2. **`src`** - путь к файлу шрифта (нпр., на сервер) и подсказываем браузеру  формат (`woff2`, `woff`, `ttf`, `otf`). можно подключать несколько форматов, браузер выберет подходящий - **но в современном вебе используют ток 1 формат woff2**

3. **`font-weight`** - **указываем враиант подклюбчаемого анчертания (жирность)** толщина шрифта (нужно писать **только стандартные значения**, иначе браузер будет игнорировать)

4. **`font-style`** - стиль (нужно писать **только стандартные значения**, иначе браузер будет игнорировать) **(обычно указывают нормал? - не захотел погруждаться сильно)**

5. **`font-display`** - **информирует браузер о том, как он должен рендерить текст когда соотвествущий шрифт не загружен** как текст показывается до загрузки шрифта:
    - `swap` - **хажает очень короткий период бшлокировки и бесконечный период замены. при такой стратегии барузщер практисекси сразу отображает текст с запансым шрфтом, а затем, когда подгружден нужный шрифт, переключается на него, иными словами свап применяют когад отображение текст является приоритетной задачей и отображдение текста ждругим шрифтом не нарушит нормальное использованипе тсраницы . под другим шрифтом подразумевается шрифт, который можно указать в фонт фемили - идущий через запятую полсе основго семейства шрфита (нпр font-famiy: 'Roboto', Arial, sans-serif; - такиъ значений может быть скок угодно)** сначала системный шрифт, потом заменяет на свой (обычно используют `swap`)
      **ключевое слово sans-serif (нпр ont-famiy: 'Roboto', Arial, sans-serif; ) браузер сам автоматичеки подберет наиболее подкодящий  шрифт без засечек который есть в ОС пользрвателоя**
    - `block` - текст скрыт до загрузки шрифта
    - `fallback` - короткая задержка, потом системный шрифт, если наш не загрузился
    - `optional` - максимально быстрая загрузка, может не показывать наш шрифт (т.е. браузер решает сам, может не загрузить наш шрифт, если загрузка идёт медленно)
6. src: local('Roboto') - здесь нужно написать парвильнео название шрифта, браузер проверит, есть ли этот шрифт на устройстве открышем этот сайт, если есть тогда применится с компа (бдует бытсрее грузитсья), прописывать необязательно
- виды шрифтов (ttf, woff2, woff etc)
# 2. `@import`

```css
/* подключение другого CSS-файла (SASS)*/
@import url('адрес до файла, который внутри себя сам будет подключать за нас шрифты (обычно берется с сайта google fonts)') - но фактически директива импорт используется для подключаения сторонних файлов стилей.
```
# 3.  тег `<link>` - обычно код беертся с сайта гугл фонтс 


# *. САМЫЙ ЛУЧШИЙ СПОСОБ ПОДКЛЮЧЕНИЕ ЭТО С ПОМОЗЬЮ ЛОКАЛЬНОГО ФАЙЛА + ФОНТ-ФЕЙС, Т.К. ЭТО ЭКОНОМИЯ ТРАФИКА СЕТИ (НЕ ПРИЙЛЕТСЯ ОБРАЩАТЬСЯ К СТОРОННЕМУ СЕРВЕРУ ДЛЯ ПОЛУЧЕНИЯ ШРИФТОВ, Т.К. ОНИ МОГУТ ОТДАВАТЬ ШРФИТЫ МЕДЛЕННЕЕ ЧЕМ ДОМЕН НАШЕГО САЙТА + МОЖЕТ ОТДАТЬ КУЧУ НЕ НУЖНЫХ ДАННЫХ НПР РАЗЛИЧНЫЕ ВАРИНТЫ ФОРМАТОВ ШРИФТОВ КОТОРЫЕ НЕ ПОНАДОБЯТС ЯБОЛЬШИНСТВУ НАШИХ ПОЛЬЗОВАТЛЕЙ И САЙТ БУДЕТ ЗАГРУЖАТЬСЯ БОЛЬШЕ ЧЕМ МОГ БЫ )





























```css
/* подключение другого CSS-файла (SASS)*/
@import "reset.css"
```


1. `@import` позволяет загружать **дополнительные CSS-файлы** в текущий

2. Обычно пишется **в начале файла**, перед всеми стилями, чтобы корректно применялись правила

💡 Часто используется для: 
	- разделения стилей на основной и мобильный
	- подключения внешних библиотек стилей



@use
@forward
