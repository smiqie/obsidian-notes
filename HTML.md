
### **Шаблон HTML-документа:**

| Тег / элемент      | Описание                                                                     |
| ------------------ | ---------------------------------------------------------------------------- |
| `<!DOCTYPE html>`  | Объявляет тип документа и версию HTML5                                       |
| `<html lang="ru">` | Корневой элемент страницы, атрибут `lang` указывает язык                     |
| `<head>`           | Метаданные страницы: кодировка, подключение стилей и скриптов, SEO-настройки |
| `<title>`          | Заголовок страницы (отображается во вкладке браузера и результатах поиска)   |
| `<body>`           | Основное содержимое страницы: текст, изображения, кнопки, блоки и т.д.       |

---
### **Комментарии:**

```html
<!-- Однострочный комментарий в HTML -->

<!-- Многострочный
     комментарий
     в HTML -->
```

---
### `ul`, `ol`, `li`

#### Ненумерованный список (`<ul>`)

```html
<!-- визуальная обёртка для маркированного списка -->
<ul>
	<!--  элемент списка (пункт) -->
	<li>Первый пункт</li>
	<li>Второй пункт</li>
</ul>
```

- В `<ol>` принято оформлять не только списки, но и любую упорядоченную последовательность: шаги рецепта, меню, кнопки, карточки, нав-меню и тд.

#### Нумерованный список (`<ol>`) - встречается редко

```html
<!-- цифровая обёртка для нумерованного списка -->
<ol>
	<!--  элемент списка (пункт) -->
	<li>Первый пункт</li>
	<li>Второй пункт</li>
</ol>
```

---

### `h1-h6`, 

```html
<!-- h2-h6 - структура контента, образуют иерархию (подзаголовки, разделы) -->
<h1>Новости</h1>
	<h2>Политика</h2>
	<h2>Технологии</h2>
		<h3>ИИ и разработки</h3>
```

- `h1` - основной заголовок страницы (SEO), может быть только один на странице
 
- `h2` обычно используются в `<section>`

---
### `p`

```html
<p>Это абзац текста.</p>
```

- В `<p>` принято оборачивать минимум 1 предложение

- **Особенность:** длинные строки без пробелов не переносятся (т.е., если в тексте нет пробелов, то слово вылезает за границы контейнера):

```html
<p>
Loremipsumdolorsitametconsectetursdgjdjsfdsgfshdgfhdgsfhdsgfhsdgfhsgdfsgdj
</p>
```

---

### `b`, `strong`

```html
<strong>Важный текст</strong> <!-- семантическое выделение, a11y -->
```

- `<strong>` - **важный текст** (скринридеры подчеркивают интонацией) ✅ a11y; можно вкладывать для усиления важности!

---

### `em`

```html
<em>Акцент</em> <!-- семантический акцент, a11y -->
```

- `<em>` - **акцент** (скринридеры меняют интонацию) ✅ a11y

---

### `mark`

```html
<mark>Выделенный текст</mark> <!-- желтый фон по умолчанию -->
```

- `mark` - **важный текст для текущего контекста пользователя** ✅ a11y
- Пример: результаты поиска, где `<mark>` выделяет совпадения с запросом

---

### `sub`, `sup`

```html
H<sub>2</sub>O <!-- подстрочный текст -->
x<sup>2</sup> <!-- надстрочный текст -->
```

- `<sub>` - подстрочный текст (химические формулы)
- `<sup>` - надстрочный текст (степени, сноски)

---

### `br`, `hr`

```html
Первая строка<br>Вторая строка <!-- перенос строки -->
<hr> <!-- горизонтальная линия -->
```

- `<br>` - **принудительный перенос** (избегать в CSS-вёрстке, но OK для адресов/стихов)
- `<hr>` - **тематический разрыв** (логический разделитель секций), не использовать как декоративный элемент!

---

### `code`, `pre`

```html
<code>const x = 10;</code> <!-- фрагмент кода в строке -->

<pre>
function hello() {
  console.log('Hi');
}
</pre> <!-- блок кода с сохранением пробелов/переносов -->
```

- `<code>` - **инлайн-код** (моноширинный шрифт)
- `<pre>` - **предформатированный текст** (сохраняет пробелы/переносы, часто с `<code>` внутри)

---
### `img`

```html
<img src="path/to/image.jpg" alt="Описание изображения" width="800" height="600" loading="lazy">
```

- `img` - вставка изображения
- **Атрибуты:**
    - `src` - путь к изображению 
    - `alt` - альтернативный текст ✅ a11y, SEO  
        Заполнять только для значимых изображений; для декора оставлять пустым `alt=""`
    - `width` / `height` - размеры в пикселях  
        Важно указывать сразу в HTML, чтобы при загрузке не было скачков контента (CLS)
    - `loading`:
        - `lazy` - изображение загружается только при скролле до него (экономит трафик)
        - `eager` - загружается сразу (по умолчанию)

---
### `a`

```html
<a href="https://example.com" target="_blank">Ссылка</a> <!-- внешняя ссылка -->

<a href="#section">Якорная ссылка</a> <!-- переход к элементу с id="section" -->

<a href="file.pdf" download>Скачать файл</a> <!-- скачивание -->

<a href="mailto:email@example.com">Написать письмо</a> <!-- открывает почтовый клиент -->
<a href="tel:+79001234567">Позвонить</a> <!-- звонок в 1 клик -->
```

- `a` - вставка ссылки (для навигации по сайту или за его пределами ✅ a11y)
- **Атрибуты:**
    - `href` - URL или якорь (`#id`)
    - `download` - скачивание файла вместо перехода
    - `mailto:` - открывает почтовый клиент пользователя
    - `tel:` - звонок по номеру (особенно на мобильных)
    - `target="_blank"` - открыть в новой вкладке  
        **Использовать для внешних ссылок на другие ресурсы**, чтобы не уводить пользователя с сайта  

---
### `button`

```html
<button type="button">Обычная кнопка</button> <!-- действие на странице -->
<button type="submit">Отправить форму</button> <!-- внутри <form> -->
<button type="reset">Сбросить</button> <!-- внутри <form> -->
```

- `button` - добавляет кнопку (Действие в рамках текущей страницы ✅ a11y; нпр., открыть модальное окно, переключить вкладку, отправить форму и тд.) 
- **Атрибут `type`:**
    - `button` - **всегда указывать по умолчанию** (избегает ошибок с формами)
    - `submit` - отправка данных формы на сервер (например, кнопка «Войти»)
    - `reset` - сброс значений формы до дефолтных (например, «Сбросить фильтры товаров»)

---

### `blockquote`

```html
<blockquote cite="https://example.com/article">
  <p>Это длинная цитата, которая занимает несколько строк текста.</p>
</blockquote>
```

- `blockquote` - блочный элемент для больших цитат ✅ a11y
- Атрибут `cite` - URL источника (не отображается визуально) ✅ a11y
- **Цитата без указания источника - плохой тон**

---

### `q`

```html
<q cite="https://example.com">Короткая цитата в строке</q>
```

- `q` - строчный элемент для коротких цитат внутри текста ✅ a11y
- Атрибут `cite` - URL источника (не отображается визуально) ✅ a11y

---

### `cite`

```html
<blockquote>
  <p>Быть или не быть — вот в чём вопрос.</p>
</blockquote>
<cite>Уильям Шекспир, «Гамлет»</cite>

<p>Как писал в своей статье <cite>«Искусство программирования»</cite>...</p>
```

- `cite` - строчный тег для источника цитаты ✅ a11y (идёт сразу после `<blockquote>` или `<q>`)
- **Имя автора ≠ источник!** Источник - это ресурс (книга, статья, сайт)
- Также используется для названий: книг, статей, картин, фильмов и других объектов искусства

---

### `video`

```html
<video src="video.mp4" controls width="640" height="360" poster="cover.jpg">
  <source src="example.mp4" type="video/mp4">
  <source src="example.webm" type="video/webm">
  <p>Ваш браузер не поддерживает видео.</p>
</video>
```

- `video` - выводит видеоплеер
- **Атрибуты:**
    - `src` - путь к видеофайлу
    - `controls` - добавляет элементы управления (play, pause, volume)
    - `width` / `height` - размеры видеоплеера
    - `autoplay` - автопроигрывание после загрузки страницы
    - `autoplay muted` - автопроигрывание без звука 
    - `loop` - начать проигрывание сначала после завершения
    - `poster` - путь до обложки (изображение до воспроизведения, иначе первый кадр)
    - `playsinline` - для мобильных (видео в своих размерах, а не в полноэкранном режиме)
- **Поддержка разных форматов:** используй `<source>` ([см. конспект](#source))

---

### `audio`

```html
<audio src="audio.mp3" controls>
  <source src="example.mp3" type="audio/mpeg">
  <source src="example.ogg" type="audio/ogg">
  Ваш браузер не поддерживает аудио.
</audio>
```

- `audio` - выводит аудиоплеер на страницу
- **Атрибуты:**
    - `src` - путь к аудиофайлу
    - `controls` - добавляет элементы управления
    - `autoplay` - автопроигрывание после загрузки страницы
    - `autoplay muted` - автопроигрывание без звука
    - `loop` - зациклить воспроизведение
- **Поддержка разных форматов:** используй `<source>` ([см. конспект](#source))

---

### `source`

```html
<video controls>
  <source src="video.mp4" type="video/mp4">
  <source src="video.webm" type="video/webm">
  Ваш браузер не поддерживает это видео.
</video>

<audio controls>
  <source src="audio.mp3" type="audio/mpeg">
  <source src="audio.ogg" type="audio/ogg">
</audio>
```

- `source` - указывает альтернативные источники медиафайлов
- **Атрибуты:**
    - `src` - путь до файла определенного формата
    - `type` - MIME-тип файла (инструкция браузеру как читать содержимое)
        - Видео: `video/mp4`, `video/webm`, `video/ogg`
        - Аудио: `audio/mpeg`, `audio/ogg`, `audio/wav`
- **FALLBACK-подход** - указание резервных источников данных, повышает вероятность воспроизведения
- Браузер выбирает первый поддерживаемый формат
- Текст после `<source>` - сообщение об ошибке, если ничего не загрузилось

---

### `iframe`

```html
<iframe 
  src="https://www.youtube.com/embed/VIDEO_ID" 
  width="560" 
  height="315" 
  title="Описание видео"
  allow="autoplay; fullscreen"
  allowfullscreen>
</iframe>
```

- `iframe` - добавляет виджет на страницу (интерактивные карты, виджет погоды, видео YouTube и тд.) ✅ a11y
- **Атрибуты:**
    - `src` - путь к встраиваемому контенту
    - `width` / `height` - размеры фрейма
    - `title` - описание содержимого iframe ✅ a11y
    - `allow` - политика разрешений (например, `autoplay`, `camera`, `microphone`)
    - `allowfullscreen` - разрешить запуск в полноэкранном режиме

---

## `table`

```html
<table>
  <caption>Продажи за 2024 год</caption>
  
  <thead>
    <tr>
      <th>Месяц</th>
      <th>Продажи</th>
      <th>Прибыль</th>
    </tr>
  </thead>
  
  <tbody>
    <tr>
      <td>Январь</td>
      <td>1000</td>
      <td>500</td>
    </tr>
    <tr>
      <td>Февраль</td>
      <td>1200</td>
      <td>600</td>
    </tr>
  </tbody>
  
  <tfoot>
    <tr>
      <td>Итого</td>
      <td>2200</td>
      <td>1100</td>
    </tr>
  </tfoot>
</table>
```

- `table` - добавляет таблицу ✅ a11y
- `caption` - заголовок таблицы ✅ a11y (может быть только один)
- `thead` - обёртка верхней части таблицы (заголовки столбцов) ✅ a11y
- `tbody` - основная часть контента таблицы ✅ a11y
- `tfoot` - нижняя часть таблицы (итоговые подсчёты, суммы) ✅ a11y
- `tr` - строка в таблице
- `th` - заголовочная ячейка ✅ a11y
- `td` - обычная ячейка таблицы

### **Атрибуты объединения ячеек:**

```html
<table>
  <tr>
    <th colspan="2">Объединённый заголовок</th> <!-- 2 ячейки по горизонтали -->
  </tr>
  <tr>
    <td rowspan="2">Высокая ячейка</td> <!-- 2 ячейки по вертикали -->
    <td>Обычная ячейка</td>
  </tr>
  <tr>
    <td>Ещё ячейка</td>
  </tr>
</table>
```

- `colspan` - объединяет несколько ячеек по горизонтали (указывает количество столбцов)
- `rowspan` - объединяет несколько ячеек по вертикали (указывает количество строк)

---

### `form`

```html
<form action="/submit" method="post">
  <fieldset>
    <legend>Личные данные</legend>
    
    <label for="name">Имя:</label>
    <input type="text" id="name" name="user-name" required>
    
    <label for="email">Email:</label>
    <input type="email" id="email" name="user-email" required>
  </fieldset>
  
  <button type="submit">Отправить</button>
</form>
```

- `form` - обёртка элементов, с которыми взаимодействует пользователь ✅ a11y
- **Атрибуты:**
    - `action` - адрес серверного скрипта-обработчика формы
    - `method` - метод отправки (`get` / `post`, по умолчанию `get`)
    - `novalidate` - отключает стандартную валидацию (используется при валидации через JS)

---

### `fieldset` + `legend`

```html
<fieldset>
  <legend>Контактная информация</legend>
  <!-- поля формы -->
</fieldset>
```

- `fieldset` - визуальная группировка нескольких полей ввода в один набор ✅ a11y
- `legend` - заголовок внутри `<fieldset>` ✅ a11y

---

### `label`

```html
<label for="login">Логин:</label>
<input type="text" id="login" name="user-login">
```

- `label` - метка к интерактивному элементу ✅ a11y (клик по тексту = фокус на поле ввода)
- **Атрибут:**
    - `for` - связка с `id` элемента формы

---

### `input`

```html
<!-- Текстовые поля -->
<input type="text" id="name" name="user-name" value="Иван">
<input type="email" id="email" name="user-email"> <!-- ✅ валидация, 📱 спец. клавиатура -->
<input type="url" name="website"> <!-- ✅ валидация, 📱 спец. клавиатура -->
<input type="tel" name="phone"> <!-- 📱 спец. клавиатура -->
<input type="search" name="query"> <!-- 📱 спец. клавиатура -->
<input type="number" name="age" min="18" max="100"> <!-- ✅ валидация, 📱 спец. клавиатура -->
<input type="password" name="pass">

<!-- Дата и время -->
<input type="date" name="birthday"> <!-- day/month/year -->
<input type="datetime-local" name="meeting"> <!-- day/month/year/time -->
<input type="month" name="period"> <!-- month/year -->
<input type="week" name="week-num">
<input type="time" name="alarm">

<!-- Специальные -->
<input type="color" name="theme"> <!-- color picker -->
<input type="file" name="avatar" multiple> <!-- загрузка файлов -->
<input type="range" name="volume" min="0" max="100"> <!-- ползунок -->

<!-- Чекбоксы и радио -->
<input type="checkbox" id="agree" name="terms" value="agreed" checked>
<label for="agree">Согласен с условиями</label>

<input type="radio" id="male" name="gender" value="male" checked>
<label for="male">Мужской</label>
<input type="radio" id="female" name="gender" value="female">
<label for="female">Женский</label>
```

- `input` - поле для ввода
- **Обязательные атрибуты:**
    - `id` - связка с `label` через `for`
    - `name` - имя поля (сервер распознаёт данные по `name`)
    - `value` - значение по умолчанию в поле ввода / значение для сервера (обязательно для `checkbox`, `radio`)
- **Типы (`type`):**
    - `text` - обычное текстовое поле (по умолчанию)
    - `email` - ✅ валидация формата, 📱 спец. клавиатура
    - `url` - ✅ валидация формата, 📱 спец. клавиатура
    - `tel` - 📱 спец. клавиатура (без валидации)
    - `search` - 📱 спец. клавиатура для поиска
    - `number` - ✅ только числа, 📱 спец. клавиатура
    - `password` - скрывает вводимые символы
    - `date` / `datetime-local` / `month` / `week` / `time` - выбор даты/времени
    - `color` - встроенный color picker
    - `file` - загрузка файлов (+ `multiple` для нескольких)
    - `checkbox` - чекбокс (+ `checked` для выбора по умолчанию)
    - `radio` - выбор одного варианта из группы (общий `name` для группы) / (+ `checked` для выбора по умолчанию)
    - `range` - ползунок (+ `min`, `max` для диапазона)

---

### `textarea`

```html
<textarea id="comment" name="user-comment" rows="5" cols="30">
Текст по умолчанию
</textarea>
```

- `textarea` - многострочное поле ввода
- **Атрибуты:**
    - `id`, `name` - стандартные атрибуты
    - `rows`, `cols` - высота/ширина в строках/символах (на практике настраивается через `css`)
    - `wrap` - форматирование текста при отправке:
        - `soft` - по умолчанию (переносы не изменяются)
        - `hard` - добавляет переносы строк

---

### `select` + `option` + `optgroup`

```html
<!-- Базовый select -->
<select id="city" name="user-city" required>
  <option value="moscow">Москва</option>
  <option value="spb">Санкт-Петербург</option>
</select>

<!-- Select с placeholder (ничего не выбрано по умолчанию) -->
<select id="city" name="user-city">
  <option value="" disabled selected>Не выбрано</option>
  <option value="moscow">Moscow</option>
  <option value="samara">Samara</option>
  <option value="petersburg">Petersburg</option>
</select>

<!-- С группировкой -->
<select id="country" name="user-country" multiple>
  <optgroup label="Европа">
    <option value="russia">Россия</option>
    <option value="germany">Германия</option>
  </optgroup>
  <optgroup label="Азия">
    <option value="japan">Япония</option>
  </optgroup>
</select>
```

- `select` - выпадающий список ✅ a11y
- **Атрибуты:**
    - `id`, `name` - стандартные атрибуты
    - `multiple` - множественный выбор (список не выпадающий)
    - `required` - обязательный выбор
- `option` - вариант списка
    - `value` - значение для сервера (обязательно)
    - `selected` - выбрано по умолчанию
    - `disabled` - недоступно для выбора
- `optgroup` - визуальная группировка опций (встречается редко)
    - `label` - название группы
- **Кастомизация:** чаще используют готовые библиотеки, т.к. через CSS стилизовать сложно

---

### **Атрибуты форм**

```html
<!-- Валидация -->
<input type="text" required> <!-- обязательное поле -->
<input type="text" minlength="3" maxlength="20"> <!-- ограничение символов -->
<input type="text" pattern="[a-zA-Z]+"> <!-- регулярное выражение -->

<!-- Состояние -->
<input type="text" disabled> <!-- неактивно, данные не отправляются -->
<input type="text" readonly> <!-- только чтение, данные отправляются -->

<!-- Подсказки -->
<input type="text" placeholder="Введите имя"> <!-- текст-подсказка -->
<input type="text" autocomplete="on"> <!-- автозаполнение (on/off) -->
<input type="text" autofocus> <!-- автофокус при загрузке -->
<input type="text" spellcheck="true"> <!-- проверка орфографии -->

<!-- Клавиатуры (📱) -->
<input type="text" inputmode="numeric"> <!-- числовая клавиатура -->
<input type="text" inputmode="email"> <!-- клавиатура для email -->
<input type="text" inputmode="tel"> <!-- клавиатура для телефона -->
<input type="text" inputmode="url"> <!-- клавиатура для URL -->
<input type="text" inputmode="search"> <!-- клавиатура для поиска -->
<input type="text" inputmode="none"> <!-- отключить клавиатуру -->

<!-- Связка с формой -->
<input type="text" form="user-info"> <!-- связка с формой вне её -->
<form id="user-info"></form>
```

**Атрибуты:**

1. **`required`** - обязательное поле (`input`, `textarea`, `select`)
2. **`disabled`** - элемент неактивен, данные не отправляются (`input`, `textarea`, `select`, `button`, `fieldset`, `option`, `optgroup`)
3. **`readonly`** - только чтение + можно наводить фокус, но данные отправляются (`input`, `textarea`)
4. **`placeholder`** - текст-подсказка (`input`, `textarea`)
5. **`inputmode`** - 📱 альтернативная клавиатура (`text`, `numeric`, `decimal`, `tel`, `email`, `url`, `search`, `none`)  
    Используй **сначала** правильный `type`, **затем** `inputmode` для уточнения
6. **`autocomplete`** - автозаполнение (`on` / `off`)
7. **`autofocus`** - автофокус при загрузке страницы
8. **`form`** - связка элемента с формой вне её (полезно при разбивке формы по дизайну)
9. **`minlength` / `maxlength`** - ограничение символов (`input`, `textarea`)
10. **`pattern`** - регулярное выражение для валидации (`input`)
11. **`spellcheck`** - проверка орфографии (`true` / `false`)
12. **`novalidate`** - отключает стандартную валидацию (для `<form>`, используется при валидации через JS)
13. **`wrap`** - форматирование текста при отправке (`soft` / `hard`, только `textarea`)