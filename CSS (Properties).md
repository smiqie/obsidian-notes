 # `box-sizing`

```css
/* Размеры НЕпредсказуемы: width/height учитывают только контент.
   При добавлении padding или border элемент становится больше. */
box-sizing: content-box;

/* Размеры предсказуемы: width/height включают контент + padding + border.
   При добавлении отступов или границ общий размер не меняется. */
box-sizing: border-box;
```
1. `box-sizing` влияет только на внутренние размеры элемента: `content`, `padding`, `border`

# `background` + подсвойства

```css
/* отвечает только за цвет фона элемента */
background-color: lightblue;

/* задаёт фоновое изображение или градиент элементу || url() - путь к изображению (относительный или абсолютный)*/
background-image: url('img/bg.jpg');
background-image: conic-gradient(red, blue);

/* позволяет накладывать изображения друг на друга (нпр. с прозрачным фоном) */
background-image:
	 url('./cat-1.png'), /* будет в самом верху */
	 url('./cat-2.png'),
	 url('./cat-3.png'); /* будет в самом низу */

/* задаёт позицию фонового изображения относительно контейнера */
background-position: center;

/* задаёт, будет ли фон повторяться на все свободное место контейнера */
background-repeat: no-repeat; 
background-repeat-x: repeat; /* отвечает только за x-ось */
background-repeat-y: repeat; /* отвечает только за y-ось */

/* задаёт размер фонового изображения относительно контейнера*/
background-size: cover;

/* задаёт правила фона при скролле (будет ли фон прокручиваться вместе со страницей) */
background-attachment: fixed; 

background: red; /* неправильно, лучше использовать background-color (т.к. background использует множество подсвойств, и этой записью мы можем их всех переопределить) - также относится к остальным св-вам (font, margin, padding и подобным)*/

/* линейный градиент */
background: linear-gradient(45deg, red, blue);
background: linear-gradient(to right, red, blue);
background: linear-gradient(red 40%, green 50%, blue);

/* радиальный градиент */
background: radial-gradient(circle, red, blue);
background: radial-gradient(red 30%, blue 70%);

/* сокращённая (shorthand) запись для всех background-свойств */
background: lightblue url('img/bg.jpg') center / cover no-repeat;
```

1. `background-repeat`:
    - `no-repeat` - изображение не повторяется
    - `repeat` - повторяется по обеим осям (по умолчанию + самое популярное значение)
    - `repeat-x` - повторяется по горизонтали
    - `repeat-y` - повторяется по вертикали
    - `space` - повторяется с равными промежутками по обеим осям, заполняя всё пространство контейнера
    - `round` - повторяется по обеим осям с растягиванием изображений для заполнения всего пространства контейнера
2. `background-position`:
	- Значение по умолчанию: `0 0`
    - Значения: `center` (самое популярное значение), `top`, `bottom`, `left`, `right` **||** `px` **||** `%` (обычно указывают в `%` или `px`) 
    - Паттерны:
        1. `background-position: X Y;` **X**: горизонталь (слева → справа), **Y**: Вертикаль (сверху → вниз) **||** если указано только одно значение, то позиционирование будет настроено только по горизонтали
        2. `background-position: <side> <offset> <side> <offset>;` **side** - сама сторона **||** **offset** - отступ от неё
3. `background-size`:
    - `cover` - картинка заполняет весь блок, сохраняя пропорции, но может обрезаться (если не помещается) - наиболее популярное значение **||** ==идеально для фонов на весь экран==
    - `contain` - картинка полностью помещается в блок, сохраняя пропорции, но в контейнере могут появиться пустые места **||** ==если нужно показать всю картинку целиком==
    - `100px 200px` - точные размеры (ширина × высота)
	    1. могут быть и другие единицы измерения (нпр. `%`, `rem` и тд.)
	    2. при задании ширины и высоты пропорции могут искажаться. чтобы сохранить пропорции, для одной из сторон нужно указать `auto`: `background-size: 100% auto;`
    - `auto` - оригинальный размер (значение по умолчанию)
4. `background-attachment`
    - `scroll` - фон прокручивается вместе со страницей (по умолчанию)
    - `fixed` - фон зафиксирован относительно родительского прокручиваемого элемента (т.е. при прокрутке страницы фон находится в определенной позиции)
5. `linear-gradient` - создаёт линейный градиент (плавный переход между цветами)
    - Паттерн: `linear-gradient(направление, цвет-1, цвет-2, ...)`
    - **Направление:**
        - угол: `45deg`, `90deg`, `180deg - по умолчанию` и т.д. (от 0 до 360)
        - turn единицы: `1turn = 360deg`, `0.5turn = 180deg` и тд.
        - ключевые слова: `to top` (0deg), `to bottom` (180deg), `to left` (270deg), `to right` (90deg), `to top right` (45deg), `to bottom right` (135deg) и т.д.
    - **Точки остановки (color stops):**
	    - 1я точка - при котором закончится плавный переход из предыдушего цвета, 2я точка - с которого начнется плавный переход в следующий цвет (нпр. `linear-gradient(blue 30%, red 40% 60%, green 90%)`). Проценты считаются от высоты элемента
        - `linear-gradient(red 40%, green 50%, blue)` - red переходит в green в диапазоне 40%-50%, остальное blue
6. `radial-gradient` - создаёт радиальный градиент (от центра к краям)
    - Паттерн: `radial-gradient(форма, цвет-1, цвет-2, ...)`
    - **Форма:** `circle` (круг) или `ellipse` (эллипс, по умолчанию)
7. `conic-gradient` - создаёт конический градиент 

# `border` + подсвойства

```css
/* полностью убирает любую границу элемента (толщину, стиль, цвет) */
border: none;

/* можно отдельно задавать каждую сторону */
border-top: 2px solid red;
border-right: 1px dashed green;
border-bottom: 3px dotted blue;
border-left: 4px double purple;
border-block: 10px solid blue; /* задаёт рамку по вертикали */
border-inline: 10px solid blue; /* задаёт рамку по горизонтали */

/* стиль рамки для отдельных сторон */
border-top-style: solid;
border-right-style: dashed;
border-bottom-style: dotted;
border-left-style: double;
border-block-style: solid;
border-inline-style: solid;

/* цвет рамки для отдельных сторон */
border-top-color: red;
border-right-color: blue;
border-bottom-color: green;
border-left-color: purple;
border-block-color: purple;
border-inline-color: purple;

/* ширина рамки */
border-width: 2px; 

/* стиль рамки */
border-style: solid;

/* цвет рамки */
border-color: black;

/* сокращённое св-во: width style color*/
border: 1px solid black;
```

1. `border-width`
	- Можно задавать толщину с помощью ключевых слов `thin, medium, thick` - **крайне не популярный формат, в большинстве случаев используют px**
	- Паттерн: ^pattern
		1. `1 значение` - все стороны одинаковые
		2. `2 значения` - верх/низ, право/лево
		3. `3 значения` - верх, право/лево, низ
		4. `4 значения` - верх, право, низ, лево
2. `border-style`
	- `none` - без границы  (по умолчанию)
	- `solid` - сплошная (самое популярное значение)
	- `dashed` - пунктир
	- `dotted` - точки
	- `double` - двойная
	- **остальные исользуются крайне редко**
3. `border-color`
	- Паттерн: [[CSS (Properties)#^pattern]]
4.  `border: none;` рекомендуется прописывать перед добавлением своей рамки, чтобы убрать дефолтные границы и избежать неожиданных эффектов


# `border-radius`

```css
/* скругление углов */
border-radius: 10px; /* со всех сторон элемента*/
border-top-left-radius: 5px;
border-top-right-radius: 10px;
border-bottom-left-radius: 15px;
border-bottom-right-radius: 20px;
```

1. `border-radius` - скругление углов у элемента
	- Отлично работает со свойством `outline`
	- Создание круга из элемента: `border-radius: 50%;`
	- Не имеет отношение к свойству `border`!
	- Можно задавать в любых единицах измерения, но **обычно используют px или %**
	- Можно перечислить до 4х значений по порядку: `верхний левый, верхний правый, нижний правый, нижний левый` **можем получить довольно интересные фигуры**
		- Например: `border-radius: 0 30px 80% 30px;`
	- Паттерн:
		1. `1 значение` - все углы одинаковы
		2. `2 значения` - верхние / нижние
		3. `3 значения` - верхний левый, верхний правый + нижний левый, нижний правый
		4. `4 значения` - верхний левый, верхний правый, нижний правый, нижний левый

# `transform` + подсвойства

```css
/* по умолчанию — элементы без трансформаций */
transform: none;

/* поворот на 45 градусов по часовой стрелке */
transform: rotate(45deg); 


/* как сказал тот чел 50% 50% по дефолту или center?*/
transform-origin: 50% 50%;

/* элемент вращается от верхнего левого угла */
transform-origin: 0 0; 

/* элемент вращается от верхнего левого угла */
transform-origin: top left; 
```

1. `transform: rotate(deg)` - вращает элемент в определенной точке [[CSS (Properties)#^rotatePoint]]
	- Значения: `deg` (самый часто используемый вариант)
2. `transform-origin` - точка вращения элемента ^rotatePoint
	- Паттерн: `transform-origin: горизонталь(X) вертикаль(Y)`
	- Значения: `center`, `top`, `bottom`, `left`, `right` **||** `%` **||** `px`
	- применется вместе со св-вами трансформации и задает положение относительно которого они применяются 


# `position`, `top`, `left`, `bottom`, `right`, `inset`

```css
/* по умолчанию - обычный поток документа */
position: static;

/* сдвигаем элемент относительно его обычного положения */
position: relative;
top: 10px;   /* вниз на 10px */
left: 20px;  /* вправо на 20px */

/* вырывает элемент из потока, позиционируется относительно ближайшего родителя с position != static */
position: absolute;
top: 0;
right: 0;

/* фиксирует элемент относительно окна браузера */
position: fixed;
top: -50px;
left: 0;

/* комбинированное поведение relative + fixed, прилипает при скролле в пределах родителя */
position: sticky;
top: 0;
```

1. `position` - расположение элементов относительно друг друга 
    - `static` - обычный поток (элемент расположен в обычно порядке), `top/left` не работают (по умолчанию)
    - `relative`
	    - сдвигает элемент **визуально** относительно своего места, **место в потоке остаётся прежним (т.е. элемент по сути остается на своём месте)**
		- само значение `relative` со свойствами смещения: `top/left inset z-index` **используется ОЧЕНЬ редко**, но **часто используется как контейнер для absolute детей**
	    - по сути элемент позиционируется также как и `static`, но на него будут работать свойства смещения: `top/left inset z-index`
	    - никак не влияет на соседние элементы
    - `absolute`
	    - **выбивает элемент из основного потока элементов** (т.е. элемент перестаёт занимать своё первоначальное положение среди своих соседних элементов)
	    - позиционируется относительно ближайшего родителя с **position ≠ static** (если такого родителя нет - относительно **body (верхний левый угол браузера)**)
	    - элемент по ширине, занимает ровно столько места - сколько в нём вмещается контента
    - `fixed`
	    - элемент позиционируется относительно границ окна браузера
	    - всегда виден при скролле
	    - вырывает элемент из потока
    - `sticky`
	    - ведёт себя как **relative** до заданного скролла, потом “прилипает” к позиции и ведёт себя как **fixed**
	    - обязательно нужно задавать `top`/`left` и тд. чтобы все работало
	    - иногда может **не срабатывать** (из-за того что элемент с `position: sticky` занимает всю высоту)
		    - **чтобы пофиксить** - нужно задать элементу с `position: sticky` **height** или **max-height**

- `top/left/right/bottom` - задают смещение элемента в любых единицах измерения, работают только для `relative`, `absolute`, `fixed`, `sticky`
	- **в реальной практике обычно используют только 2 свойства смещения:** по оси X: `left/right` &  по оси Y: `top/bottom`
- `inset` - современное shorthand-свойство для позиционирования, объединяет `top`, `right`, `bottom`, `left` в одну запись
	- **Паттерн:**
		1. `inset: x` - для всех сторон
		2. `inset: x x` -  верх/низ, право/лево
		3. `inset: x x x` - верх, право/лево, низ
		4. `inset: x x x x` - верх, право, низ, лево

- Использование`position` нужно когда нельзя обойтись margin/padding. например: слои, всплывающие элементы, фиксированные кнопки и тд.

# `overflow`

```css
/* содержимое выходит за пределы контейнера (по умолчанию) */
overflow: visible;  
/* скрывает все, что выпирает за границы контейнера. САМОЕ ПОПУЛЯРНОЕ ЗНАЧЕНИЕ */
overflow: hidden;    
/* скрывает все, что выпирает за границы контейнера, но у блока появлвется прокурутка повзорляющая проскролить и увидеть все его внутренее сождержимое // полоса прокуртки будет в любом случае, даже если конент влезает в гарницы элементы. т.е. значение auto более умное - будет отобрадать полосы прокутки ток когда она реально нужна */
overflow: scroll;  

overflow: auto; /* аналогично значению scroll но прокрутка будет появляться ток тогда когда осждердимое блока действительно выпирает за его размеры. лучше его юзать чем scroll*/

overflow: clip; /* работает почти как hidden но намного строже. т.к. если у нас hidden то мы все равно мождем программно чреез js прокуртить скрутое осдердимое блока, когда clip это не позволит сделать */

overflow: visible (ось x) visible (ось y); == overflow-x: visible; и overflow-y: visible; - задаем сразщу для двух осей отдельно 


баг с обрезщанием: overflow: visible hidden; фикс бага: overflow: visible clip;
```

1. `overflow` - управляет видимостью содержимого, выходящего за границы блока // определяет правила того, как будет вести себя блочный элемент если его содержимое не влезает в его размеры 

# `padding` + подсвойства

```css
/* задаёт внутренние отступы - пространство между контентом и границами элемента */
padding: 20px;

/* задаёт отступы отдельно для каждой стороны */
padding-top: 10px;
padding-right: 15px;
padding-bottom: 20px;
padding-left: 25px;

/* для горизонтальных отступов (влево/вправо) */
padding-inline: 50px;

/* для вертикальных отступов (вверх/вниз) */
padding-block: 50px;
```
 
- задаёт величину внутреннего отступа от **края элемента (border)** до внутреннего содержимого **(content)**
- значения не могут быть отрицательными
- по умолчанию входит в общую ширину/высоту элемента, если не используется `box-sizing: border-box;`    
- Паттерн: 
		1. `padding: x` - отступы со всех сторон
		2. `padding: x x` -  верх/низ, право/лево
		3. `padding: x x x` - верх, право/лево, низ
		4. `padding: x x x x` - верх, право, низ, лево

# `margin` + подсвойства

```css
/* задаёт внешние отступы - пространство вокруг элемента */
margin: 20px;

/* отдельные стороны */
margin-top: 10px;
margin-right: 15px;
margin-bottom: 20px;
margin-left: 25px;
margin-block: 50px;

/* центрирование блока по горизонтали */
margin-inline: auto;
```

- задаёт величину внешнего отступа вокруг элемента (от границы `border` до соседних элементов или границ контейнера)
- **margin** в отличие от **padding** не является частью самого элемента  
  (не влияет на его фактический размер, но влияет на то, сколько места он занимает в потоке)
- значения могут быть отрицательными (иногда бывает полезным)
- браузер может **“схлопывать” margin’ы**, если два соседних блока имеют вертикальные отступы - берётся большее из них
- Паттерн: (аналогичен [padding](#padding) ↑)
- `auto` - автоматический отступ. браузер распределяет свободное пространство между левым и правым отступом поровну, и элемент оказывается по центру
- отступы задаются **сверху вниз и слева направо** - элемент не может “толкать себя”, его двигают соседи
# `margin` & `padding` + content

![[box_model.png]]

- `content` - область, где отображается содержимое элемента (ширина и высота определяются `width` и `height`)
- **margin**: нужно **отделить элемент от других** (создать расстояние снаружи блока)
- **padding**: нужно **оттолкнуть контент внутрь от границ блока**, т.е. создать пространство внутри элемента - между контентом и его рамкой

# `display` + виды элементов

```css
/* Элемент отображается как блочный */
display: block;

/* Элемент отображается как строчный */
display: inline;

/* Элемент отображается как строчно-блочный */
display: inline-block;

/* Элемент полностью скрывается со страницы */
display: none;

/* Элемент становится flex-контейнером */
display: flex;

/* Элемент становится inline flex-контейнером */
display: inline-flex;
```

1. `block`
    - элемент начинается с новой строки
    - занимает всю доступную ширину родителя
    - высота подстраивается под внутренний контент (`height: auto`)
    - можно задавать width, height, margin, padding etc
    - может содержать в себе блочные и строчные элементы
    - 💡Задавать фиксированные размеры (`width`, `height` в `px`) - плохая практика, так как контент может измениться (например, текст станет длиннее) и выйдет за рамки блока
2. `inline`
    - элемент располагается в одну строку со своими соседями (если не хватает места по ширине - переносятся на новую строку)
    - ширина и высота подстраиваются под контент
    - нельзя задавать `width`, `height` и вертикальные отступы (`margin-top`, `margin-bottom`) - не работают
    - можно использовать горизонтальные отступы (`margin-left`, `margin-right`) и padding
    - может содержать в себе только строчные элементы
3. `inline-block`
    - элементы ведут себя как строчные - располагаются в одну строку
    - при этом можно задавать width, height, margin, padding
    - ширина и высота по умолчанию подстраиваются под контент
    - может содержать строчные и блочные элементы
4. `none`
    - элемент полностью скрывается со страницы
    - не занимает место в потоке (как будто его нет)
    - в HTML-разметке элемент остается (можно снова показать через CSS или JavaScript)
    - используется для скрытия элементов (меню, всплывающих окон, модалок и т.д.)
    - 💡 **Отличие от `visibility: hidden`:** `visibility: hidden` скрывает элемент, но сохраняет место в потоке, а `display: none` - убирает элемент полностью
5. `flex`
    - элемент становится flex-контейнером
    - все дочерние элементы становятся flex-элементами
    - основное преимущество - удобное выравнивание и распределение элементов по горизонтали и вертикали (в отличие от обычных блоков)
    - контейнер ведёт себя как **блочный элемент** (занимает всю ширину родителя, начинается с новой строки)
6. `inline-flex`
    - элемент становится flex-контейнером
    - контейнер ведёт себя как **строчный элемент** (располагается в строку с другими элементами, ширина подстраивается под содержимое)
    - 💡 В 95% случаев используется `flex`. `inline-flex` нужен редко - только когда flex-контейнер должен быть частью строки текста (кнопки с иконками и тд.)

|Тип элемента|Расположение|Управление размером|Можно вложить|Начинается с новой строки|
|---|---|---|---|---|
|`block`|один под другим|✅ `width` / `height`|любые|✅|
|`inline`|в одну строку|❌|только строчные|❌|
|`inline-block`|в одну строку|✅|строчные / блочные|❌|
|`none`|не отображается|—|—|—|
|`flex`|задаёт гибкий контейнер|✅|любые|✅|
|`inline-flex`|задаёт строчный flex-контейнер|✅|любые|❌

# `display: flex` + все его свойства + технология Flexbox

```css
/* === СВОЙСТВА FLEX-КОНТЕЙНЕРА === */
display: flex;                 
flex-direction: row;           
justify-content: space-between;
align-items: center;           
align-content: space-around;   
flex-wrap: wrap;               
flex-flow: row wrap;      
gap: 20px;

/* === СВОЙСТВА FLEX-ЭЛЕМЕНТОВ === */
align-self: flex-end;          
order: 1;                  
```

1. `flex-direction` - задает направление главной (основной) оси
	- `row` - слева направо (по умолчанию)
	- `row-reverse` - справа налево
	- `column` - сверху вниз
	- `column-reverse` - снизу вверх

2. `justify-content` - выравнивает элементы по **главной оси**
	- `flex-start` - элементы прижаты к началу оси (по умолчанию)
	- `flex-end` - элементы прижаты к концу оси
	- `center` - по центру
	- `space-between` - первый и последний у краёв контейнера, промежутки между элементами равные
	- `space-around` - равные отступы между всеми элементами, кроме первого и последнего (у них, от краёв контейнера, отступ =  половине промежутка между элементами) **в реальной работе нужно применять редко, а не злоупортеблять**
	- `space-evenly` - равные отступы между **всеми элементами**. **в реальной работе нужно применять редко, а не злоупортеблять**

3. `align-items` - выравнивает элементы вдоль **поперечной/побочной оси**
	- `stretch` - растягиваются на всю высоту контейнера (по умолчанию)
	- `flex-start` - прижаты к началу поперечной оси
	- `flex-end` - прижаты к концу поперечной оси
	- `start` / `end` - сокращённая запись вместо `flex-start`/`flex-end`, адаптируется к направлению языка (RTL/LTR) - полезно для мультиязычных сайтов
	- `center` - по центру поперечной оси **самое популярное значение**
	- `baseline` - выравниваются по базовой линии текста **бывает полезен при верстке блоков, по типу списка характеристик товара**
		 ![[Pasted image 20260113111454.png]]

4. `align-content` - задаёт свободное пространство между **рядами элементов по поперечной оси** (если `flex-wrap: wrap`).💡 Не работает, если только одна строка элементов. **в реальной практике применяется редко**
	- `stretch` - ряды одинаково растягиваются по контейнеру и занимают одинаковое пространство (по умолчанию)
	- `flex-start` - ряды прижаты к началу контейнера (поперечной оси)
	- `flex-end` - ряды прижаты к низу/концу контейнера (поперечной оси)
	- `center` - все ряды по центру
	- `space-between` - первый ряд прижат к началу, последний к концу поперечной оси, а остальные ряды распределятся равномерно (при их наличии)
	- `space-around` - ==см. justify-content==
	- `space-evenly` - ==см justify-content==

5. `align-self` - переопределяет align-items для конкретного элемента
	- `auto` - наследует от контейнера (по умолчанию)
	- `stretch` - растягивается на всю высоту
	- `flex-start` - прижат к началу
	- `flex-end` - прижат к концу
	- `center` - по центру
	- `baseline` - по линии текста

6. `flex-wrap` - задаёт, должны ли элементы переноситься на новую строку при нехватке места (адаптивность)
	- `nowrap` - все элементы в одну строку (по умолчанию)
	- `wrap` - элементы переносятся на новую строку
	- `wrap-reverse` - переносятся, но в обратном порядке (новые строки сверху). **крайне не популярное значение!**

7. `flex-flow` - объединяет `flex-direction` и `flex-wrap` в одно свойство
	- Паттерн: `flex-flow: <direction> <wrap>;`
	- Пример: `flex-flow: row wrap;`

8. `order` - задаёт визуальный порядок расположения элемента внутри контейнера 
	- значение: любое целое число (`0` по умолчанию)
	- чем больше число - тем **позже** элемент идёт в порядке
	- отрицательные значения - элемент будет **раньше**
	- порядок не влияет на HTML-разметку, только на визуальное отображение
	- **злоупотреблять не стоит. в реальной практике обычно используют -1 чтобы переместить определенный элемент в начало и 1 чтобы переместить определенный элемент в конец**

9. `gap` - задаёт промежутки между элементами внутри flex-контейнера
    - любые длины: `px`, `em`, `%` и т.д. (по умолчанию: 0)
    - `gap: <по вертикали> <по горизонтали>` - шорткат для `row-gap` и `column-gap`
    - можно задавать отдельно:
        1. `row-gap` - промежуток между **рядками**
        2. `column-gap` - промежуток между **колонками**

# `text-align`

```css
/* текст по левому краю (по умолчанию) */
text-align: left;

/* текст по правому краю */
text-align: right;

/* текст по центру */
text-align: center;

/* текст по ширине (растягивается по всей ширине контейнера) */
text-align: justify;
```

- выравнивает текст внутри блока (работает только с блочными и inline-block элементами)
- **Значения:**
    - `left` - по левому краю (по умолчанию)
    - `right` - по правому краю
    - `center` - по центру
    - `justify` - по ширине (добавляет пробелы между словами для заполнения всей ширины)

# `font-size`

```css
font-size: 16px;
font-size: 100%; 
font-size: large;
```

- задаёт размер шрифта
# `font-weight`

```css
/* числовые значения */
font-weight: 100; /* Thin */
font-weight: 200; /* Extra Light */
font-weight: 300; /* Light */
font-weight: 400; /* Normal / Regular */
font-weight: 500; /* Medium */
font-weight: 600; /* Semi Bold */
font-weight: 700; /* Bold */
font-weight: 800; /* Extra Bold */
font-weight: 900; /* Black */
```

- задаёт начертание/толщину (жирность) шрифта (обычно указывают в цифрах)

# `font-style`

```css
/* обычный шрифт (по умолчанию) */
font-style: normal;

/* курсив - специальный курсивный вариант шрифта*/
font-style: italic;

/* наклонный - искусственный наклон обычного шрифта, если нет italic */
font-style: oblique;
```

- задаёт начертание шрифта

# `font-family`

```css
/* конкретный шрифт */
font-family: 'Arial';

/* несколько шрифтов (fallback) */
font-family: 'Roboto', 'Arial', sans-serif;

/* обязательные типы шрифтов (в конце) */
font-family: serif;      /* с засечками */
font-family: sans-serif; /* без засечек (встречается чаще)*/
font-family: monospace;  /* моноширинный */
font-family: cursive;    /* рукописный */
font-family: fantasy;    /* декоративный */
```

- задаёт семейство шрифта (если шрифт состоит из нескольких слов, его нужно оборачивать в кавычки (одиночные или двойные))
- **Fallback-механизм:** если первый шрифт недоступен, браузер использует следующий
- 💡 последним всегда указывать общий тип: `serif`, `sans-serif`, `monospace`

# `font`

```css
/* shorthand: style weight size/line-height family */
font: italic 700 16px/1.5 'Arial', sans-serif;

/* минимальная запись: size family */
font: 16px 'Arial', sans-serif;

/* с line-height */
font: 16px/1.5 sans-serif;
```

- сокращённая запись для всех font-свойств
- **Паттерн:** `font: [style] [weight] size[/line-height] family;`. `size` и `family` - обязательные

# `width`, `height`, `max-width`, `min-width`, `max-height`, `min-height`

```css
/* задаёт фиксированную ширину элемента */
width: 500px;

/* задаёт фиксированную высоту элемента */
height: 300px;

/* максимальная ширина - элемент не может быть больше */
max-width: 1000px;

/* минимальная ширина - элемент не может быть меньше */
min-width: 500px;

/* максимальная высота - элемент не может быть больше */
max-height: 600px;

/* минимальная высота - элемент не может быть меньше */
min-height: 300px;
```

1. `width` и `height` - задают **фиксированные** размеры элемента
    
2. `min-height` - задаёт **минимальную** высоту
    - элемент не может быть меньше указанного значения
    - если контент больше - элемент растягивается
    - 💡 Полезно для гибкости: `min-height: 640px` - блок минимум 640px, но растянется если контента больше
    - зачастую применяется, когда нужно сделать секцию высотой как минимум текущего окна браузера --> пример
3. `max-height` - задаёт **максимальную** высоту
    - элемент не может быть больше указанного значения
    - может быть меньше, если контент помещается
    - зачастую используется в комбинации со св-вом переполнения `overflow-y` и его значением `auto`
4. `min-width` - задаёт **минимальную** ширину
    - элемент не может быть меньше указанного значения
    - может растягиваться шире при наличии места
    - 💡 Пример: `min-width: 1000px` - контейнер минимум 1000px, но растянется на большом экране
5. `max-width` - задаёт **максимальную** ширину
    - элемент не может быть больше указанного значения
    - подстраивается под экран, если он меньше
    - 💡 Адаптивность: `max-width: 1000px` - на больших экранах максимум 1000px, на маленьких - сжимается
- 💡 **min/max не задают размеры, а ограничивают их** - работают как рамки, в пределах которых элемент может меняться

# `z-index`

```css
/* значение по умолчанию (0 или auto) */
z-index: 0;

/* элемент выше других */
z-index: 10;

/* элемент ниже других */
z-index: -1;
```

- задаёт порядок наложения элементов по **z-оси** ( работает только для элементов с `position` ≠ `static`)
- **обычно в значении оперируют с числами от -1 до 1000**
-  значения `z-index` принято указывать те, которые кратны 100 (нпр. 100, 200, 300, 1000) - **лучшая практика**
-  💡 **Важный нюанс:** Если у родителя задан `z-index`, он создаёт **новый контекст наложения**. Дочерние элементы "заперты" внутри этого контекста - они могут соревноваться по `z-index` только друг с другом, но не могут выйти "ниже" или "выше" родительского слоя
- Контекст наложения - набор правил, на основе которых браузер решает, какой элемент на страницу будет выше или ниже других
	![[as.png]]

---

# `outline` + подсвойства

```css
/*shorthand: width style color */
outline: 2px solid red;

/* отдельные свойства */
outline-width: 2px;
outline-style: solid;
outline-color: blue;

/* смещение контура от границы элемента */
outline-offset: 5px;

/* убрать outline */
outline: none;
```

1. `outline` - создаёт контур (обводку) вокруг элемента **со всех сторон** (не влияет на его фактические размеры в отличии от `border`)
    - Паттерн: `outline: width style color;`
2. `outline-width` - толщина контура
3. `outline-style` - стиль контура
    - `none` - без контура
    - `solid` - сплошная
    - `dashed` - пунктир
    - `dotted` - точки
    - `double` - двойная
4. `outline-color` - цвет контура
5. `outline-offset` - расстояние между контуром и границей элемента
	- может быть отрицательным (контур внутрь)
	- по умолчанию значение `0`
	- можно задавать в любых единицах измерения, но **обычно задают в px**

💡 `outline` чаще используется для **фокуса** на интерактивных элементах (кнопки, ссылки, инпуты)

---

# `box-shadow`

```css
/* горизонталь вертикаль размытие растяжение цвет */
box-shadow: 10px 10px 5px 0px rgba(0, 0, 0, 0.5);

/* несколько теней через запятую */
box-shadow: 
  5px 5px 10px red,
  -5px -5px 10px blue;

/* внутренняя тень */
box-shadow: inset 5px 5px 10px rgba(0, 0, 0, 0.3);

/* убрать тень */
box-shadow: none;

/* несколько теней одновременно */
box-shadow: 
  5px 5px 10px rgba(0, 0, 0, 0.3),
  -5px -5px 10px rgba(255, 0, 0, 0.3);
```

**Паттерн:** `box-shadow: x y blur spread color;`
- для указания значения можно использовать любые единицы измерения, но **чаще используют px**
1. **x** (horizontal offset) - смещение по горизонтали
    - положительное значение → тень вправо
    - отрицательное → тень влево
2. **y** (vertical offset) - смещение по вертикали
    - положительное значение → тень вниз
    - отрицательное → тень вверх
3. **blur** (blur radius) - радиус размытия (необязательно)
    - `0` - чёткая тень (по умолчанию)
    - чем больше - тем более размытая (меньше 0 задать не получится)
4. **spread** (spread radius) - растяжение тени (необязательно)
    - положительное → тень больше элемента
    - отрицательное → тень меньше элемента
    - `0` - тень размером с элемент
5. **color** - цвет тени
6. **inset** (необязательно) - делает тень **внутренней** (добавляется в начало)
💡 `box-shadow` не влияет на размеры элемента и не занимает место в потоке

# `border-collapse`

```css
/* границы ячеек отделены друг от друга (по умолчанию) */
border-collapse: separate;

/* границы ячеек объединены в одну */
border-collapse: collapse;
```

- `border-collapse` - управляет отображением границ ячеек таблицы (работает только с `<table>`)
- **Значения:**
    - `separate` - границы ячеек раздельные (по умолчанию)
    - `collapse` - границы ячеек объединяются в одну линию

💡 Почти всегда используется `border-collapse: collapse;` для таблиц, чтобы убрать двойные границы

---

# `list-style-type`

```css
/* убрать маркеры списка */
list-style-type: none;

/* стандартные маркеры для <ul> */
list-style-type: disc;   /* закрашенный круг (по умолчанию) */
list-style-type: circle; /* пустой круг */
list-style-type: square; /* квадрат */

/* стандартные маркеры для <ol> */
list-style-type: decimal;      /* 1, 2, 3... (по умолчанию) */
list-style-type: decimal-leading-zero; /* 01, 02, 03... */
list-style-type: lower-roman;  /* i, ii, iii... */
list-style-type: upper-roman;  /* I, II, III... */
list-style-type: lower-alpha;  /* a, b, c... */
list-style-type: upper-alpha;  /* A, B, C... */
```

- задаёт вид маркера для списков (`<ul>`, `<ol>`)
- **для `<ul>`:**
    - `disc` - закрашенный круг (по умолчанию)
    - `circle` - пустой круг
    - `square` - квадрат
- **для `<ol>`:**
    - `decimal` - арабские цифры (по умолчанию)
    - `lower-alpha` / `upper-alpha` - буквы (a, b, c / A, B, C)
    - `lower-roman` / `upper-roman` - римские цифры (i, ii, iii / I, II, III)
- `none` - убрать маркеры

💡 На практике часто используют `list-style-type: none;` и создают кастомные маркеры через CSS

---

# `text-decoration`

```css
/* убрать подчёркивание (например, у ссылок) */
text-decoration: none;

/* подчёркивание */
text-decoration: underline;

/* надчёркивание */
text-decoration: overline;

/* зачёркивание */
text-decoration: line-through;

/* несколько линий одновременно */
text-decoration: underline overline;

/* с цветом и стилем */
text-decoration: underline wavy red;
```

- управляет декоративными линиями текста
- **Значения:**
    - `none` - без декорации
    - `underline` - подчёркивание
    - `overline` - линия сверху
    - `line-through` - зачёркивание
- **можно комбинировать:** цвет, стиль (`solid`, `dashed`, `dotted`, `wavy`)

---

# `line-height`

```css
/* относительное значение (множитель от размера шрифта) */
line-height: 1.5; /* рекомендуется */

/* абсолютное значение */
line-height: 24px;

/* в процентах */
line-height: 150%;

/* значение по умолчанию */
line-height: normal; /* ~1.2 */

/* пример */
p {
  font-size: 16px;
  line-height: 1.5; /* высота строки = 24px (16 × 1.5) */
}
```

- задаёт **межстрочный интервал** (высоту строки текста)
- **Значения:**
    - коэффициент (`1.5`) - множитель от `font-size` (рекомендуется)
    - `px`, `em`, `rem` - фиксированная высота
    - `%` - процент от `font-size`
    - `normal` - браузерное значение (~1.2)

💡 **Лучшая практика:** использовать числа без единиц (`line-height: 1.5;`), чтобы значение наследовалось правильно

---

# `aspect-ratio`

```css
/* соотношение сторон 16:9 */
aspect-ratio: 16 / 9;

/* квадрат (1:1) */
aspect-ratio: 1 / 1;

/* соотношение сторон 4:3 */
aspect-ratio: 4 / 3;

/* автоматическое (по умолчанию) */
aspect-ratio: auto;

/* пример */
.video-container {
  width: 100%;
  aspect-ratio: 16 / 9; /* высота подстроится автоматически */
}
```

- задаёт **соотношение сторон** элемента (ширина к высоте)
- элемент сохраняет пропорции при изменении размера
- **Значения:**
    - `width / height` (например, `16 / 9`)
    - `auto` - без фиксированного соотношения (по умолчанию)

💡 Полезно для видео, изображений, карточек, чтобы сохранить пропорции

---

# `object-fit`

```css
/* изображение заполняет контейнер, сохраняя пропорции (может обрезаться) */
object-fit: cover;

/* изображение полностью помещается в контейнер, сохраняя пропорции */
object-fit: contain;

/* изображение растягивается/сжимается, заполняя контейнер (пропорции могут нарушиться) */
object-fit: fill;

/* изображение не изменяется, сохраняет оригинальный размер */
object-fit: none;

/* изображение масштабируется как none или contain (выбирается меньший размер) */
object-fit: scale-down;

/* пример */
.card-image {
  width: 300px;
  height: 200px;
  object-fit: cover; /* изображение заполнит блок, сохраняя пропорции */
}
```

- управляет тем, как **изображение или видео** заполняет контейнер с фиксированными размерами (работает только с `<img>`, `<video>`)
- **Значения:**
    - `cover` - заполняет контейнер, сохраняя пропорции (может обрезаться) **||** идеально для фонов и превью
    - `contain` - полностью помещается в контейнер, сохраняя пропорции (могут быть пустые места) **||** показать всё изображение
    - `fill` - растягивается на весь контейнер (пропорции могут нарушиться) (по умолчанию)
    - `none` - оригинальный размер (может выходить за границы)
    - `scale-down` - выбирает между `none` и `contain` (меньший результат)

💡 Чаще всего используется `object-fit: cover;` для изображений в карточках, чтобы они заполняли контейнер без искажений

---

# `color`

```css
/* именованные цвета */
color: red;

/* HEX (шестнадцатеричный формат) */
color: #ff0000; /* красный */
color: #00f;    /* синий (сокращённая запись) */

/* RGB (красный, зелёный, синий) */
color: rgb(255, 0, 0); /* красный */

/* RGBA (с прозрачностью) */
color: rgba(255, 0, 0, 0.5); /* полупрозрачный красный */
```

- задаёт **цвет текста** элемента

---

# `letter-spacing`

```css
h1 {
  letter-spacing: 3px; /* заголовок с увеличенным межбуквенным интервалом */
}
```

- задаёт **расстояние между символами** (межбуквенный интервал)
- **Значения:**
    - `normal` - стандартное расстояние (по умолчанию)
    - `px`, `em`, `rem` - любые единицы измерения
    - можно использовать отрицательные значения для сжатия текста

---

# `background-clip`

```css
/* градиентный текст */
.gradient-text {
  background: linear-gradient(45deg, red, blue);
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent; /* текст должен быть прозрачным */
}

/* изображение внутри текста */
.image-text {
  background-image: url('pattern.jpg');
  background-clip: text;
  -webkit-background-clip: text;
  color: transparent;
}
```

- определяет, **до какой области** блочной модели элемента распространяется фон (**редко используется, но знать полезно**)

- **Значения:**
    1. **`border-box`** - фон доходит до внешнего края границы (включая border) (по умолчанию)
    2. **`padding-box`** - фон доходит до внешнего края padding (не включает border)
    3. **`content-box`** - фон только в области контента (фон не распространяется на padding и border)
    4. **`text`** - фон обрезается по форме текста
        - требует `color: transparent;` для видимости эффекта
        - нужен префикс `-webkit-` для Chrome/Safari
        - идеально для градиентного текста
---

# `scroll-behavior`

```css
/* мгновенная прокрутка всей старницы(по умолчанию) */
scroll-behavior: auto;

/* плавная прокрутка всей страницы*/
scroll-behavior: smooth;

/* пример для всей страницы */
html {
  scroll-behavior: smooth;
}

/* пример для конкретного контейнера */
.scrollable-container {
  scroll-behavior: smooth;
  overflow-y: scroll;
}
```

- управляет **поведением прокрутки** при программной навигации (например, при клике на якорные ссылки)
- !определяет то, как будет вести себя анимация скролла после нажатия на якорную ссылку или после применения програмного скролла через js!
- !за прокрутку всей страницы отвечает элемент html, т.е. это св-во применется к нему!
- **Важно:** Свойство работает только для программной прокрутки (якоря, JavaScript), т.е. не влияет на ручную прокрутку колесиком мыши или тачпадом
- Лучше всего добавлять на `html` элемент для применения ко всей странице
- **Значения:**
    - `auto` - мгновенная прокрутка (по умолчанию)
    - `smooth` - плавная анимированная прокрутка

---

# `filter`

```css
/* без фильтров (по умолчанию) */
filter: none;

/* размытие */
filter: blur(5px);

/* яркость (0 = чёрный, 1 = норма, >1 = светлее) */
filter: brightness(0.5); /* затемнить на 50% */
filter: brightness(1.5); /* осветлить на 50% */

/* контрастность (0 = серый, 1 = норма, >1 = больше контраста) */
filter: contrast(200%);

/* оттенки серого (0 = цветной, 1 = чёрно-белый) */
filter: grayscale(100%);
filter: grayscale(0.5); /* 50% серого */

/* инверсия цветов (0 = норма, 1 = инверсия) */
filter: invert(1);

/* насыщенность (0 = обесцвечено, 1 = норма, >1 = насыщеннее) */
filter: saturate(2);

/* оттенок (поворот цветового круга) */
filter: hue-rotate(90deg);
filter: hue-rotate(180deg);

/* сепия (старое фото) (0 = норма, 1 = полная сепия) */
filter: sepia(100%);

/* прозрачность (0 = прозрачно, 1 = непрозрачно) */
filter: opacity(0.5);

/* тень (аналог box-shadow, но учитывает форму, включая прозрачные пиксели) */
filter: drop-shadow(5px 5px 10px rgb(0 0 0 / 0.5));

/* множественные фильтры (применяются по порядку) */
filter: brightness(1.2) contrast(1.1) saturate(1.3);
filter: grayscale(100%) brightness(1.2);


/* ========== ПРИМЕРЫ ========== */

/* эффект "старое фото" */
.vintage {
  filter: sepia(80%),
		  brightness(0.9),
		  contrast(1.1);
}

/* размытый фон */
.blur-background { filter: blur(10px); }

/* чёрно-белое изображение */
.bw-image { filter: grayscale(100%); }

/* неоновый эффект при наведении */
.neon:hover {
  filter: brightness(1.5) saturate(2) hue-rotate(45deg);
}

/* эффект свечения */
.glow {
  filter: brightness(1.3) drop-shadow(0 0 20px rgba(255, 255, 255, 0.8));
}

/* размытый фон (для модальных окон) */
.backdrop { filter: blur(8px) brightness(0.7); }

/* неоновый текст */
.neon-text {
  filter: drop-shadow(0 0 10px #0ff) 
          drop-shadow(0 0 20px #0ff) 
          drop-shadow(0 0 30px #0ff);
}
```

- применяет **графические эффекты** к элементу (работает с любыми элементами, включая изображения, текст, контейнеры)

- **Основные функции:**
    
    1. **`blur(radius)`** - размытие по Гауссу***
        - `radius` - радиус размытия (**обычно указывают `px`**)
        - чем больше значение - тем сильнее размытие
    2. **`brightness(amount)`** - яркость***
	    - **Значения:** в формате коэффициента  или %
        - `0` - полностью чёрный
        - `1` - оригинал (100%)
        - `>1` - светлее
    3. **`contrast(amount)`** - контрастность***
	    - **Значения:** в формате коэффициента  или %
        - `0` - полностью серый
        - `1` - оригинал (100%)
        - `>1` - больше контраста
    4. **`grayscale(amount)`** - оттенки серого (черно-белый эффект)***
        - `0` - цветное (0%) - по умолчанию
        - `1` или `100%` - чёрно-белое`
    5. **`invert(amount)`** - инверсия цветов***
        - `0` - оригинал (0%)
        - `1` или `100%` - полная инверсия
    6. **`saturate(amount)`** - насыщенность***
        - `0` - обесцвеченное
        - `1` - оригинал
        - `>1` - более насыщенное
    7. **`hue-rotate(angle)`** - поворот оттенка (сдвиг цветов на `x°`)***
        - значение в `deg` (градусах)
        - `0deg` - оригинал
        - `360deg` - полный круг (снова оригинал)
    8. **`sepia(amount)`** - эффект сепии (старое фото)***
        - `0` - оригинал (0%)
        - `1` или `100%` - полная сепия
    9. **`opacity(amount)`** - прозрачность***
    	- **Значения:** в формате коэффициента  или %
        - `0` - полностью прозрачно (0%)
        - `1` - полностью непрозрачно (100%)
        - **Интересный факт:** Работает быстрее свойства `opacity` за счёт аппаратного ускорения (но разница ничтожна)
    10. **`drop-shadow(offset-x offset-y blur-radius color)`** - тень***
        - в отличие от `box-shadow`, учитывает форму элемента, включая прозрачность
        - идеально для PNG с прозрачным фоном (**Частый приём**)
        - Паттерн: `drop-shadow(x y blur color)`



# `opacity` - управляет прозрачностью элемента (default: 1)
- знаечние: число от 0 до 1
-  элемент прозрачный но с ним можно взаимодействовать

# `visibility` - управляет прозрачностью элемента + запрещает люоое взаимодействие с ним (default: visible)
- знаечние: `visible` - элемент полностью видимый, hidden - элемент полностью невидимый + с этим элементом нельзя будет взщаимодейстовать
-  еще одно отличие от opacity: нельзя добавить плавный переход из одного состояние в дрпугое1


# `cursor`
- позволяет менять внешний вид курсора при использованиии на устройстве мыши (работает при наведении?)
- auto -  значение по умолчанию (браузер сам решает какого виды курсор отобразить). для большинства элементов это стандартная стрелка, для ссылок - рука с указательным пальцем, для полей ввода - вертикальная черта
- популярные значения:
	1. none - курсор при наведении не будет виден вовсе
	2. default - стандартный курсор, стандартная стрелка
	3. text - курсор вертикальной черты
	4. pointer - меняет курсор на руку (не рекомендуется применять к кнопкам!!! т.к. получится некоректный ux. изначально это значения обычно добавляется для ссылок)
- не такие популярные значения:
	1. all-scroll - иконка скролла во все стороны
	2. alias - загнутая стрелка, атк иногда обозначают ссылки ведущие на сторонние сайты
	3. cell - иконка выбора ячеек таблицы
	4. col-resize - иконка изменения размера табличной колонки
	5. context-menu - стандартный курсор с добалвением иконки списка, обычно добавляют на меню открывающиеся при клике на ПКМ
	6. copy - стандартный курсор с добавлением иконки зеленого плюсика
	7. crosshair - иконка с крестом, обычно добавляют для изображений, когда в нем можно выделить опредленную область
	8. grab - иконка с рукой, демонстрирующая возможность перетаскивания
	9. grabbing - иконка с рукой демонстрирующая состояние когда содержимое уже схавтаили для перетаскивания
	10. help - иконка со знаком вопроса, используется для вспомогательной инфы
	11. move - иконка со стрелками во все стороны демонстрирующая возможность двигать элемент
	12. no-drop - стандартный курсор с перечеркнутым кругом демонстрирующим его невозможность перетащить файл в область элемента
	13. not-allowed - такой же курсор как и no-drop но предположительно на других ОС будет выглядеть чутка иначе
	14. progress - стандартный курсор с кружком статуса загрузки
	15. row-resize - иконка изменения размера табличной строки
	16. vertical-text - иконка похожая на значение text но перевернутая по вертикали 
	17. wait - иконка аналогичная иконке progress 
	18. zoom-in - иконка с увеличивающей лупой
	19. zoom-out - иконка с уменьшающей мастаб лупой
	20. `cursor: url('cat.png'), auto;` - установка своего кастомного курсора через url(). Например: формата png или svg и размера не более 32\*32px. и чтобы это все заработало нужно написать запасное значение (fallback)


# `user-select`
- определяет то, можно ли выделить текст в элементе 
- самые важные знаечния: 
	1. auto - значение по умолчанию. браузер сам определитт правила выделения для конретного типа элемента / в большинстве случаев текст может быть выделен
	2. none - отклбчает возможность выделять текст в элементе, может пригодиться для некоторых интерактивных элементов, таких как кнопки, тчобы при взамодействии случайно не выделялся текст (т.к. у них по умолчанию текст выделяется)

# `pointer-events` 
- отвечает за то, может ли пользователь нажимать на элемент
- по умолчанию значение auto
- значение none - отключает возможность нажатия на элемент
- это св-во наследуется всеми дочерними элементами!
```html
<div class="box">
	<button>Click</button>
</div>	
```
```css
.box{
	pointer-events: none; /* также применится к детям */
}
button{
	pointer-events: none; /* переопределили знаечние, чтобы на кнопку можно было нажать */	
}
```
- очень выручает нпр тогда когда интерактивный элемент вкладывается в другой и нужно точечно отключить взаимодействие с другим слоем!!!


# `resize`
- укащывает правило того, как можно изменять размеры элемента с помощью манипуляций перетаскивания правого нижнего уголка. зачастую применяется к элементу textarea, но также можно применить к любому элементу у которого задан не стандартный overflow (нпр, со значением hidden)
- значения:
	1. none - значение по умолчанию для большинства элементов, при котором размеры элемента изменять нельзя и никакой иконки уголка у элемента не будет
	2. both - значение по умолчнаию для textarea, позволяет менять размер в обе стороны
	3. horizontal - включает возможность изменять размеры ток по горизонтали
	4. vertical - включает возможность изменять размеры ток по вертикали (зачастую это самое удобное с точки зрения ux поведение для элемента textarea!!!)


# `transform` 
- отвечает за трансофрмации (смещение, мастабирование, перемещение и тд)
- трансформации на элементе не влияют на положение, т.е. соседи будут всегда видеть элемент там где прописано его изначальное положение без трансофрмаций
- знаечния:
	1. оп умолчанию none (к элементу не применены трансформации)
0. смещение
	1. transalteX(50px) - смещает элемент по горизонтали на указанное внтури скобок значение. любые единицы измерения + значения могут быть отрицательные, тогда смещение будет в обратную сторону
	2. transalteY(-3px) - смещает элемент по вертикали, если отрицательное значение то по вертикали вверх
	3. transalte(12px, 45px) - смещает элемент по двум осям <x><y> 
1. масштабирвоание
	1.  scaleX(2) - масштабирвоание по x-оси // в отрицательном значении эдемент будет отзеркален по горизонтали
	2.  scaleY(1.2) - масштабирвоание по y-оси // в отрицательном значении эдемент будет отзеркален по вертикали
	3.  scale(1.3, -0.5) - масштабирвоание по двум осям с сохранение пропорций, что нам и нуэно в большиснтве сулчаев, <x><y> или scale(3) - если значения совпадают
	4. в значение прописывается число в виде коэфициента (по умолчанию значение 1 - т.е. ориг размер элемента)
2. поворот элемента
	1. rotate() - поворот элемента. по муолчанию значение 0deg ь.е. 0 гардусов и элемент не повернут. 360deg = полный поворот. знаечния могут быть полождиетльными или отрицательными (элемент будет прокручиваться против часовой стрелки). или еще есть единца измерение turn, 1turn = 1му полному поворотуБ 0.5turn = 180 гардумсоув. если указать к примеру 45deg то элемент повернется на 45 граудусов по часовой стрелке отноистельно координат своего центра
3. искажение элемента
	1. skewX(0deg) - по оси х
	2. skewY(21deg) - по оси y
	3. skew(12deg, 31deg) - по двум осям сразу <x> <y>
	4. в качестве значения указывеатся отрицательное или положительное число в deg или  turn
4. можем перечилсять несколько разных фугнкций трансформаций черещ пробел чтобы все они применились к одному элементу nsitiondelay:`transform: translate(30px, -30px) scale(1.5) skew(20deg, 5deg);`
5. также не обязщатиел ьно использовать функции и transform, т.к. появились отдельные удобные свойства transalte (0 0 -default), scale(1 1 -default), rotate(0deg -default). перво езначение ось икс второе ось игрек или же одно значение только для икс оси / нпр `translate: 30px 12px;` или `transalte: 12px; - x ось` // со scale вес также но при ождном значении будет дейстовать масштабирвоание по 2м осям // в rotate просто указываетс яодно число


# `transition - добавление плавности для переходов`
```css
transition: all 0s ease 0s; - обобщенное св-во (значение по умолчанию )

/* аналогично */
transition-property: all; - имя ксс св-ва на котрое мы хотим влияеть, или же ключевое слово all если хотим добавить плавности всем св-вам
transition-duration: os; - длительность переъода
transition-timing-function: ease; - функция сокрости ихменения св-ва
transition-delay: 0s; - длительность задержки переде началом изменения св-в
```
- св-во transition нужно задавать элементу котоырй будет видоизменять свои ксс параметры и применять это св-во необходимо на элемент в статичном и не в ховер состоянии!!  
- параметры перехода transition могут работаь не ток со свойтсвмаи трансофрмации, но и со многими дургими анпример с color, background-color, box-shdow, filter, outline-color, top, right, bottom etc

- мождешм делать так: `transition: 1s; - вес остальные подсвойства будут иметь значение по деолфту` или `transition: 1s 2s;`
- `transition-timing-function` значения
	1. ease, ease-in, ease-out, ease-in-out, linear, cubic-bezier(0.1, 0.6, 1.0, 0.1) - эта функция описывеатся со специальнойц формулой из  4х точек перечиленнызх через запятую. формально все перечиленные клбчевые слова это функции основнные на формуле cubic-bazer просто их созадли для удобства (шорткаты)