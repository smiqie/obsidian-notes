# CSS (Селекторы, каскад, специфичность и наследование)

```css
/* каскад */
p { color: blue; }
p { color: red; } /* применится red (ниже в коде) */

/* специфичность */
p { color: blue; }           /* вес: низкий */
.text { color: green; }      /* вес: выше */
#main { color: red; }        /* вес: самый высокий */

/* наследование */
.parent {
	color: coral;
}
<div class="parent">
  <p>Текст</p> /* дочерний <p> унаследует color: coral */
</div>
```

1. **Селекторы** - ключи доступа к определенным элементам в разметке

2. **Каскад** - механизм, определяющий порядок применения стилей к элементам
    - каждый элемент может иметь несколько правил стилей
    - каскад определяет, какие правила применятся, основываясь на приоритетности, специфичности и источнике
    - позволяет наследовать свойства от родительских элементов и переопределять их в дочерних

3. Источники стилей:
   ![[sourcesOfStyles.png]]

4. **Специфичность (вес правила)** - определяется по типу и сложности селектора, а также по положению в коде (чем ниже, тем главнее при равном весе)
    - чем выше вес, тем главнее правило (главное правило доминирует над остальными)

5. **Наследование** - способность вложенных элементов перенимать свойства своих предков
	- ![[styleInheritance.png]]
   
   
# Типы селекторов

## 1. Селектор по тегу

```css
/* выбирает все элементы указанного типа */
p {
  color: blue;
}
```

```html
<p>Этот текст будет синим</p>
```

---

## 2. Селектор по классу

```css
/* выбирает все элементы с указанным классом */
.button {
  background-color: blue;
}
```

```html
<button class="button">Кнопка</button>
```

---

## 3. Селектор по идентификатору

```css
/* выбирает элемент с указанным id */
#header {
  background-color: red;
}
```

```html
<header id="header">Шапка сайта</header>
```

💡 **Не рекомендуется использовать для стилизации (плохой тон)!** ID имеют очень высокую специфичность и затрудняют переопределение стилей

---

## 4. Селектор по атрибуту

```css
/* базовый селектор - выбирает все элементы с указанным атрибутом */
[placeholder] {
  border: 1px solid gray;
}

/* элементы с конкретным значением атрибута */
[type="tel"] {
  color: green;
}

/* можно без кавычек */
[type=email] {
  color: blue;
}
```

```html
<input type="text" placeholder="Введите имя">
<input type="tel" placeholder="Телефон">
<input type="email" placeholder="Email">
```

- позволяет найти элементы на странице по любому атрибуту
- **самый многофункциональный из всех селекторов**, позволяет делать изящные выборки

### 4.1. `[attr*=value]` - содержит подстроку

```css
/* выбирает элементы, где атрибут содержит указанный текст */
[href*="hh.ru"] {
  color: orange;
}

[class*="btn"] {
  padding: 10px;
}
```

```html
<a href="https://hh.ru/">hh.ru</a>
<a href="https://spb.hh.ru/">spb.hh.ru</a>
<button class="btn-primary">Кнопка</button>
<button class="submit-btn">Отправить</button>
```

💡 Удобен для добавления иконок ссылкам на основе содержания href

### 4.2. `[attr^=value]` - начинается с

```css
/* выбирает элементы, где атрибут начинается с указанного значения */
[href^="https://"] {
  color: green;
}

[href^="http://"] {
  color: red;
}

[class^="icon-"] {
  font-size: 20px;
}
```

```html
<a href="https://youtube.com">Безопасная ссылка</a>
<a href="http://example.com">Небезопасная ссылка</a>
<span class="icon-home">🏠</span>
<span class="icon-user">👤</span>
```

- '^' - "циркумфлекс", или "крышечка"
### 4.3. `[attr$=value]` - оканчивается на

```css
/* выбирает элементы, где атрибут оканчивается на указанное значение */
[href$=".pdf"] {
  color: red;
}

[href$=".fr"] {
  font-weight: bold;
}

[src$=".png"] {
  border: 2px solid blue;
}
```

```html
<a href="document.pdf">PDF файл</a>
<a href="https://google.fr">Французский Google</a>
<img src="logo.png" alt="Логотип">
```

### 4.4. `[attr~=value]` - содержит отдельное слово

```css
/* выбирает элементы с определенным словом в атрибуте (отделённым пробелами) */
[alt~="cat"] {
  border: 3px solid orange;
}

[class~="active"] {
  background-color: yellow;
}
```

```html
<img alt="fox, cat" src="cat1.jpg"> <!-- подходит -->
<img alt="fox, notcat" src="cat2.jpg"> <!-- не подходит -->
```

- ищет **отдельные слова**, разделённые пробелами (слово должно быть изолировано с обеих сторон)
- '~' - "тильда"

### 4.5. `[attr operator value i]` - игнорирование регистра

```css
/* позволяет игнорировать регистр в значении атрибута */
[alt*="cat" i] {
  border: 2px solid red;
}

[type="text" i] {
  background-color: lightblue;
}
```

```html
<img alt="BIG FAT CAT" src="cat1.jpg"> <!-- подходит -->
<img alt="small cat" src="cat2.jpg"> <!-- подходит -->

<input type="TEXT"> <!-- подходит -->
<input type="text"> <!-- подходит -->
```

### 4.6. `[attr|=value]` - начинается со значения или значение-префикс

```css
/* выбирает элементы, где:
   значение атрибута точно совпадает 
   или 
   начинается с указанного значения + тире */
[class|="car"] {
  color: blue;
}

[lang|="en"] {
  font-style: italic;
}
```

```html
<div class="car">Совпадает</div>
<div class="car-tesla-2023">Совпадает</div>
<div class="cars">Не совпадает</div>

<p lang="en">Подходит</p>
<p lang="en-US">Подходит</p>
<p lang="english">Не подходит</p>
```

---

## 5. Селектор потомка (пробел)

```css
/* цепочка селекторов через пробел - обращается к вложенным элементам */
div ul li {
  list-style-type: none;
}
```

```html
<div>
  <ul>
    <li>Без маркера</li>
    <li>Без маркера</li>
  </ul>
</div>
```

- выбирает **все вложенные элементы** на любом уровне глубины
- 💡 **делать цепочку слишком большой - плохой тон**, в большинстве случаев лучше использовать классы

---

## 6. Группировка селекторов (запятая)

```css
/* плохая читаемость */
h1, h2, h3 {
  color: red;
}

/* хорошая читаемость */
h1,
h2,
h3 {
  color: red;
}
```

```html
<h1>Заголовок 1</h1>
<h2>Заголовок 2</h2>
<h3>Заголовок 3</h3>
```

- применяет одинаковые стили к нескольким селекторам
- 💡 **лучше каждый селектор располагать на отдельной строке** для улучшения читаемости

---

## 7. Объединение селекторов (без пробела)

```css
.button {
  background-color: red;
}

.button.is-active {
  background-color: orange;
}

div.container {
  width: 100%;
}

a.link.external {
  color: blue;
}
```

```html
<button class="button">Обычная кнопка</button>
<button class="button is-active">Активная кнопка</button>

<div class="container">Контейнер-div</div>

<a class="link external" href="#">Внешняя ссылка</a>
```

- позволяет обращаться к элементам, у которых совпала цепочка классов/тегов (обращается к элементам с несколькими классами одновременно)
- 💡 **делать цепочку слишком большой - плохой тон**, не стоит усложнять код

---

## 8. Универсальный селектор (\*)

```css
* {
  margin: 0;
  padding: 0;
}
```

- выбирает **все элементы** на странице
- **не затрагивает псевдоэлементы** `::before` и `::after`
- 💡 **не стоит злоупотреблять им**: например, если нужно задать шрифт для всех элементов, лучше прописать его в `body` и свойство будет наследоваться **практически** всеми элементами (т.к. свойство и так наследуемое)

---

## 9. Селектор прямых дочерних элементов (>)

```css
section > h2 {
  color: blue;
}

.container > .item {
  margin: 10px;
}
```

```html
<section>
  <h2>Будет синим</h2>
  <h2>Будет синим</h2>
  <div>
    <h2>НЕ будет синим (не прямой потомок)</h2>
  </div>
</section>
```

- выбирает элементы, которые находятся **непосредственно внутри родителя** (на одном уровне вложенности)

---

## 10. Смежный селектор (+)

```css
/* выбирает элемент, следующий сразу после указанного */
h2 + p {
  font-weight: bold;
}

.title + .subtitle {
  margin-top: 0;
}

input + label {
  color: green;
}
```

```html
<h2>Заголовок</h2>
<p>Этот параграф будет жирным (следует сразу за h2)</p>
<p>Этот параграф обычный</p>
<p>И этот тоже обычный</p>
```

- выбирает **только один элемент**, который следует **сразу же** после указанного на том же уровне вложенности
- элементы должны быть **соседями** (siblings)

---

## 11. Родственный селектор (~)

```css
/* выбирает ВСЕ последующие элементы на том же уровне */
h2 ~ p {
  color: gray;
}

input:checked ~ label {
  color: green;
}
```

```html
<h2>Заголовок</h2>
<p>Этот параграф серый</p>
<p>И этот серый</p>
<div>Это не параграф</div>
<p>Этот параграф тоже серый</p>
```

- в отличие от смежного селектора `+`, выбирает **ВСЕ последующие элементы**, лежащие на том же уровне вложенности
- элементы должны быть **родственными** (siblings)

# Лушчий выбор в качестве селектора - селектор по классу (т.к. это золотая середина по специфичности + гибкость)