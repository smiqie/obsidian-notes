# Ключевые слова

```css
/* именованные цвета */
color: red;
background-color: blue;
border-color: green;
color: black;
color: white;
```

- именованные цвета - набор предопределенных названий цветов
- примеры: `red`, `blue`, `green`, `black`, `white`, `coral`, `crimson` и т.д.

---

# `rgb()`

```css
/* базовый синтаксис: канал красного, канал зеленого, канал синего */
color: rgb(255 0 0); /* красный */
background: rgb(0 255 0); /* зеленый */
border-color: rgb(0 0 255); /* синий */

/* с альфа-каналом (прозрачность) */
color: rgb(255 0 0 / 0.5); /* красный с 50% прозрачностью */
background: rgb(0 0 0 / 0.25); /* черный с 25% прозрачностью */
border-color: rgb(100 150 200 / 0.8); /* синеватый с 80% непрозрачностью */
```

- **Паттерн:** `rgb(красный зеленый синий)` или `rgb(красный зеленый синий / альфа)`
- **Каналы:**
    - 1-е число - канал красного
    - 2-е число - канал зеленого
    - 3-е число - канал синего
    - диапазон: от `0` до `255` (чем больше число, тем сильнее влияние этого канала на итоговый цвет)
    - цвет формируется за счет смешивания 3-х каналов
- **Альфа-канал:** добавляется через `/` после трех каналов
    - отвечает за прозрачность
    - диапазон: от `0` (полностью прозрачный) до `1` (непрозрачный)
    - примеры: `0.25` = 25% прозрачности, `0.5` = 50% прозрачности

---

# `rgba()` - устаревший вариант

```css
/* устаревший синтаксис с запятыми */
color: rgba(255, 0, 0, 0.5);
background: rgba(0, 0, 0, 0.25);
border-color: rgba(100, 150, 200, 0.8);
```

- **Паттерн:** `rgba(красный, зеленый, синий, альфа)`
- формально работает, но **лучше использовать современный синтаксис `rgb()` с альфа-каналом**

---

# HEX-code

```css
/* полная запись */
color: #FF0000; /* красный */
background: #00FF00; /* зеленый */
border-color: #0000FF; /* синий */

/* сокращенная запись */
color: #F00; /* = #FF0000 */
background: #0F0; /* = #00FF00 */
border-color: #990; /* = #999900 */

/* с альфа-каналом (4-я пара символов) */
background: #FF0000C7; /* ~75% прозрачности */
color: #00FF0080; /* зеленый с ~50% прозрачности */
```

- **HEX-code** - шестнадцатеричный код цвета RGB-модели
- **Паттерн:** `#RRGGBB` - каждая пара символов отвечает за свой цветовой канал
    - 1-я пара - красный канал
    - 2-я пара - зеленый канал
    - 3-я пара - синий канал
- **Символы:** используются цифры от `0` до `9` и латинские буквы от `A` до `F` (можно использовать любой регистр)
- **Сокращение:** если в каждой группе каналов по 2 одинаковых символа, можно сократить запись до 3 символов
    - `#F00` = `#FF0000`
    - `#990` = `#999900`
    - `#ABC` = `#AABBCC`
- **Альфа-канал:** можно добавить как 4-ю пару символов
    - пример: `#FF0000C7` (C7 ≈ 75% прозрачности)
    - 💡 такой способ задания прозрачности не очень интуитивно понятен, поэтому **чаще используют `rgb()` для создания прозрачности**

---

# `hsl()`

```css
/* базовый синтаксис */
color: hsl(0 100% 50%); /* красный */
background: hsl(120 100% 50%); /* зеленый */
border-color: hsl(240 100% 50%); /* синий */

/* с альфа-каналом */
color: hsl(0 100% 50% / 0.5); /* красный с 50% прозрачностью */
background: hsl(200 80% 50% / 0.8); /* синий с 80% непрозрачностью */

/* примеры управления насыщенностью */
background: hsl(200 80% 50%); /* насыщенный синий */
background: hsl(200 20% 50%); /* ненасыщенный (серый) синий */

/* примеры управления светлотой */
background: hsl(200 80% 20%); /* темный синий */
background: hsl(200 80% 50%); /* средний синий */
background: hsl(200 80% 80%); /* светлый синий */
```

- **HSL** - способ задания цвета через оттенок, насыщенность и светлоту
- **Паттерн:** `hsl(оттенок насыщенность% светлота%)` или `hsl(оттенок насыщенность% светлота% / альфа)`
- **hue (оттенок):**
    - значение от `0` до `360` (градусы цветового круга)
    - `0` / `360` = красный, `120` = зеленый, `240` = синий
- **saturation (насыщенность):**
    - от `0%` до `100%`
    - `0%` = серый (полностью ненасыщенный)
    - `100%` = максимально насыщенный цвет
- **lightness (светлота):**
    - от `0%` до `100%`
    - `0%` = черный
    - `50%` = средний (обычно его и задают)
    - `100%` = белый
- **Альфа-канал:** добавляется через `/` после светлоты 

**Преимущества HSL:**

- RGB и HEX - привычные способы, но не всегда при беглом прочтении кода понятно, какой код символов отвечает за какой цвет
- HSL решает эту проблему - синтаксис воспринимается проще
- вся мощь этого способа раскрывается при реализации цветовых палитр через CSS-переменные и управлении параметрами насыщенности и светлоты + идеален для создания светлой и темной темы сайта
- 💡 **HSL - более профессиональный способ задания цвета, поэтому лучше использовать его**

---

# `transparent`

```css
/* делает цвет полностью прозрачным */
color: transparent;
background: transparent;
border-color: transparent;
outline-color: transparent;
```

- ключевое слово `transparent` задает полностью прозрачный цвет (буквально делает текст/цвет невидимым)

---

# `currentColor`

```css
/* базовое использование */
.element {
  color: coral;
  border: 2px solid currentColor; /* border будет coral */
  outline: 3px solid currentColor; /* outline будет coral */
}

/* для фона */
.element {
  color: blue;
  background: currentColor; /* фон будет blue */
}


/* наследование от родителя */
.parent { 
    color: red;
}

.child { 
/* border будет red (унаследован от родителя) */ 
    border: 2px solid currentColor; 
}
```

- значение `currentColor` является ссылкой на значение цвета, которое указано в **свойстве `color`**
- это ключевое слово используется для задания цвета фону, рамке, обводке, а также для перекрашивания внутренностей SVG

**Практический пример: SVG + currentColor**

```html
<button class="button">
	<svg width="36" height="36" viewBox="0 0 36 36">
		<path d="M21.518 6.62458H21.513V6.43582V6.43582..." />
		<path d="M29.4639 5.37036H26.2396V8.53466V8.53466..." />
		<path d="M36 8.45728H39.8357V11.6216H36V8.45728..." />
		<path d="M31.5555 23.6216H28.3912V23.7859V23.7859..." />
		<path d="M29.3166 19.4714H26.1523V23.6357V23.6357..." />
		<path d="M26.28 23.6399H23.1157V26.8042H26.28..." />
		<path d="M25.4637 3.89286H24.2794V6.25834V6.25834..." />
		<path d="M26.2559 34.3277C25.6918 32.4558..." />
	</svg>
</button>
```

```css
.button {
  color: tomato; /* задаём базовый цвет кнопки */
}

svg * {
  fill: currentColor; /* все path внутри SVG будут tomato */
}
```

- не нужно задавать цвет каждому элементу SVG отдельно
- 💡 такая практика с `currentColor` часто используется разработчиками