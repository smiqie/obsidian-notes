### `button`

```html
<button type="button">Обычная кнопка</button> <!-- действие на странице -->
<button type="submit">Отправить форму</button> <!-- внутри <form> -->
<button type="reset">Сбросить</button> <!-- внутри <form> -->
```

- `button` - добавляет кнопку (Действие в рамках текущей страницы ✅ a11y; нпр., открыть модальное окно, переключить вкладку, отправить форму и тд.) 
- **Атрибут `type`:**
    - `button` - **всегда указывать по умолчанию** (избегает ошибок с формами)
    - `submit` - отправка данных формы на сервер (например, кнопка «Войти»)
    - `reset` - сброс значений формы до дефолтных (например, «Сбросить фильтры товаров»)
- `title`


# Глобальные атрибуты HTML
## `class`

```html
<div class="card active">Несколько классов</div>
```

- добавляет класс элементу для стилизации через CSS или работы с JS

## `id`

```html
<section id="contacts">Контакты</section>
<a href="#contacts">Перейти к контактам</a>
```

- добавляет уникальный идентификатор элементу (должен быть уникален на всей странице)
- используется для:
    - якорных ссылок (`<a href="#id">`)
    - связки `<label for="id">` с `<input id="id">`
    - работы с элементом через JS

## `title`

```html
<button type="button" title="Добавить в корзину">🛒</button>
<abbr title="HyperText Markup Language">HTML</abbr>
<a href="/profile" title="Перейти в профиль">👤</a>
```

- всплывающая подсказка при наведении курсора ✅ a11y
- **хороший тон:** добавлять всем интерактивным элементам без текста (иконки, кнопки с символами)
- полезно для пользователей, особенно для иконок без подписей

## `tabindex`

```html
<!-- фокусируемый блок (0 = порядок по умолчанию) -->
<div tabindex="0">Блок можно выделить через Tab</div>

<!-- приоритетный порядок -->
<input type="text" tabindex="1">
<button type="button" tabindex="2">Кнопка</button>
<a href="#" tabindex="3">Ссылка</a>

<!-- исключить из навигации -->
<button type="button" tabindex="-1">Скрыто от Tab</button>
```

- задаёт порядок фокусировки элементов при навигации клавишей **Tab** ✅ a11 (делает любой элемент фокусируемым)
- **Значения:**
    - `0` - элемент фокусируется в естественном порядке (браузер сам определяет)
    - `1, 2, 3...` - задаёт приоритетный порядок (числа должны идти логически)
- **Лучшая практика:** использовать `tabindex="0"` везде, чтобы браузер сам определял порядок

## `lang`

```html
<html lang="ru">
  <body>
    <p>Это русский текст.</p>
    <blockquote lang="en">This is English quote.</blockquote>
  </body>
</html>
```

- указывает язык содержимого элемента ✅ a11y, SEO
- помогает браузеру: правильно подобрать шрифт, настроить пунктуацию (кавычки, дефисы), подключить переводчик, озвучить текст для скринридеров
- пишут обычно один раз в корневом элементе `<html lang="ru">`, но для иностранного текста внутри страницы тоже полезно указывать

## `data-*`

```html
<!-- пользовательские атрибуты для JS -->
<input type="tel" data-mask="phone" data-region="ru">
<button type="button" data-product-id="12345" data-category="electronics">
  Купить
</button>
<div data-state="active" data-count></div>
```

- пользовательские атрибуты для хранения данных
- **должны начинаться с `data-`**
- значение может быть любым или отсутствовать вовсе
- используются для:
    - передачи данных в JS (например, через `dataset`)
    - настройки библиотек/плагинов
    - хранения состояния элемента

```js
// чтение в JS
const input = document.querySelector('input');
console.log(input.dataset.mask); // "phone"
console.log(input.dataset.region); // "ru"
```

# `span`

```html
<p>Цена: <span class="price">1999₽</span></p>
```

- `span` - контейнер для строчных элементов (используется для стилизации текста)
- 💡 Если есть подходящий семантический тег (`<strong>`, `<em>`, `<mark>`, `<code>` и т.д.) - лучше использовать его вместо `<span>`


---

---

# 🗂️ Тег `<div>`

```html
<div class="container">
  <p>Содержимое внутри контейнера</p>
</div>
```

- `<div>` - контейнер (оболочка) для других элементов, не имеющий визуального оформления (по умолчанию)

```css
div {
  width: auto;   /* по умолчанию 100% ширины родителя */
  height: auto;  /* подстраивается под содержимое */
  border: 1px solid #000; /* для наглядности */
}
```

- `<div>` - блочный элемент, по умолчанию растягивается на всю ширину родительского элемента, высота подстраивается под содержимое

---

ПОДОЙДЕТ ДЛЯ ОБЩЕГО ФАЙЛА ХТМЛ И КСС!!!
## 3️⃣ Когда использовать `<img>` vs `background-image`

| Использование           | `<img>`                                   | `background-image`                                                              |
| ----------------------- | ----------------------------------------- | ------------------------------------------------------------------------------- |
| Часть контента          | ✅ Да                                      | ❌ Нет                                                                           |
| Для оформления          | ❌ Нет                                     | ✅ Да                                                                            |
| SEO (поисковые системы) | ✅ Учитывается                             | ❌ Не индексируется                                                              |
| Управление через CSS    | 🔸 Ограничено                             | 🔹 Полный контроль                                                              |
| Примеры                 | Логотипы, иконки, фотографии              | Фоны, декоративные элементы                                                     |

---


ТОЖЕ ФАЙЛ ОБШИЙ ДЛЯ ХТМЛ И КСС Я ДУМАЮ (ХОТЯ БМ НЕКОТОРЫЕ ШУТКИ ДАЖЕ ЕЩЕ ОБОБЩИТЕЛЬНЕЕ БУДУТ - НА УВРОНЕ ПРОЕКТА И ТД)
### **Относительные пути** - ссылаются **относительно текущего файла**:

```html
<!-- Путь от текущей папки (лучше читается) -->
<img src="./img/example.png" alt="">

<!-- То же самое, но без "./" (менее явно) -->
<img src="img/example.png" alt="">


<!-- Выход на уровень выше -->
<img src="../img/example.png" alt="">
```

### **Абсолютные пути** - указывают **относительно корня сайта** (корневой папки проекта):

```html
<!-- Всегда начинаются со слеша "/", путь считается от корня проекта -->
<img src="/img/example.png" alt="">
<a href="/pages/about.html">О нас</a>
```

### **Полные URL (внешние ресурсы)**

```html
<img src="https://example.com/img/photo.jpg" alt="">
<a href="https://google.com">Google</a>
```


## **Работа со шрифтами в CSS**

**Шрифт** - это набор символов (букв, цифр, знаков препинания) определённого начертания и стиля, который определяет визуальный облик текста на странице
### **Шрифты с засечками (Serif) или без (Sans-serif)**

- **Serif (с засечками)** - шрифты с небольшими "хвостиками" на концах букв  
    Используются: в печатных изданиях, книгах, для классического/формального стиля
    
- **Sans-serif (без засечек)** - шрифты с чистыми, ровными линиями  
    Используются: в веб-дизайне (лучше читаются на экранах), для современного/минималистичного стиля


## **Иконочные шрифты**

**Иконочные шрифты** - это шрифты, где вместо букв находятся иконки/символы (стрелки, логотипы, значки соцсетей и т.д.)

**Преимущество**: их можно редактировать как обычный текст через CSS-свойства:

```css
.icon {
  font-size: 24px;      /* размер */
  color: #ff0000;       /* цвет */
  font-weight: bold;    /* жирность */
}
```


# 📁 Структура проекта

#### Папка `src/` (source) - содержит все исходные материалы

- `css/` (стили)
- `js/` (скрипты)
- `fonts/` (шрифты)
- `img/` (изображения)
- `icons/` (иконки)
- `index.html` - главная страница (`index.html` - стандартное имя главной страницы, браузеры открывают её первой)
- `другие .html страницы`

- `dist/` (distribution) - готовая и оптимизированная версия сайта
-  `src` - черновик,  `dist` - финальная сборка сайта

💡 **Названия папок и файлов рекомендуется писать с маленькой буквы через дефис** (например: `my-site`, `main-page.html`), чтобы избежать проблем при загрузке на хостинг (некоторые серверы чувствительны к регистру)

💡Общепринятое правило: папка `img` - растровые изображения. папка `icons` - векторные изображения

---


ЭТО МБ В ОБЩИЙ ФАЙО Я ХЗ
- векторная графика - изображение созданное на основе множества точек, объединенных математическими формулами 
- растровая графика - ?

- jpg - фоторграии, полезный для пользваотеля окнтент, напрмер изображение товара, превью к новости и подобное
- png - фотка без фона (с прозрачным)

---


---

одинорчныйе теги улчше закрывать (лучше читается)

`<input>` no
`<input />` yes 

---

лучше юзать ссылки на файл напрямую ведущшщие на локальные файлы а не url (чтобы файлы не потяерляиьс)

---

ъорошая парктика?
интереактивные элементы не омгут содердать в себе бьлоные элементы 
нпр 
```
<button>
	<p>lfd</p>
</button>	
/* неапврильно */
```
