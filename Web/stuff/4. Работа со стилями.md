## 🔹Селекторы

**Селектор** - способ выбрать элементы (теги) HTML, к которым применяются CSS-стили

---
## 🔹 Виды селекторов

- **Селектор класса (`.class`)** - выбирает элементы по классу.  **Приориет:** 1 (высокий)
```css
.button-1 { color: red; } /* длинные имена классов обычно называют через тире */
```

- **Селектор тега (`tag`)** - выбирает **все** элементы указанного тега.  **Приориет:** 2 (средний)
```css
p { font-size: 16px; }
```

- **Универсальный селектор (`*`)** - выбирает **ВСЕ** теги (элементы) на странице. **Приориет:** 3 (низкий)
```css
* { margin: 0; padding: 0; }
```

- **User Agent Stylesheet** - встроенные браузером стили по умолчанию. **Приориет:** 4 (самый низкий)
```css
/* нпр., браузер сам делает тег <p> черным и с отступами, если не задано CSS */
```

#### 🔹 Пример конфликта приоритетов:

```css
/* Тег <div> с классом .box-1 получает красный фон */
.box-1 { background-color: red; }
/* Все теги <div> получают синий фон */
div { background-color: blue; }
```

- **Результат:**  `<div class="box-1">` будет иметь _красный фон_, потому что селектор класса имеет _более высокий приоритет_, чем селектор тега.  Все остальные `<div>` без класса будут синими

--- 
## 🔹 Подключение стилей

#### 1️⃣ Через `<link>` (внешний файл)

```html
<link rel="stylesheet" href="style.css">
```

- `rel="stylesheet"` - говорит, что это таблица стилей
- `href="style.css"` -  путь к файлу `style.css`

💡 **Файл `.CSS` не обязательно должен лежать в одной папке с HTML**:

  ```html
  <!-- путь правильный! относительно места, где лежит HTML файл-->
  <link rel="stylesheet" href="../css/style.css">
  ```
  
  -  `../` - переход на уровень выше в файловой структуре
#### 2️⃣ Через `<style>` (встроенные стили)

```html
<head>
    <style>
        p { color: blue; }
        .box-1 { background-color: red; }
    </style>
</head>
```

- Обычно тег `<style>` пишут в теге `<head>`, т.к. браузер читает код сверху вниз, стили применяются до отображения элементов

```
<link rel="icon" href="icon.png"> 
подключение фав-иконки
```