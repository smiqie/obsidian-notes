## ✍️ Свойства

### 🔸 Свойство `color`

```css
p {
  color: #333;
}
```

- Определяет цвет текста (для любых текстовых элементов: `p`, `h1–h6`, `a`, `span`, `li` и тд.)
####  Способы задания цвета

| Формат             | Пример                            | Описание                          |
| ------------------ | --------------------------------- | --------------------------------- |
| **HEX**            | `#FFE2B0`                         | Шестнадцатеричный код             |
| **RGB**            | `rgb(255, 0, 0)`                  | Red Green Blue                    |
| **RGBA**           | `rgba(0, 0, 0, 0.5)`              | Red Green Blue Alpha (Alpha: 0–1) |
| **Ключевые слова** | `darkred`, `yellow`, `lightgreen` | Названия цветов                   |
### 🔸 Свойство `text-align`

```css
.container {
  text-align: center;
}
```

- `text-align` - горизонтальное выравнивание всего содержимого внутри блока (текста, `inline`-элементов, `inline-block`, изображений и тд.)
	- `left` - по левому краю (по умолчанию)
	- `right` - по правому краю
	- `center` - по центру

### 🔸Свойство `font-size`

```css
h2 {
  font-size: 24px;
}
```

- Устанавливает размер шрифта
    - `px` - пиксели 

### 🔸 `font-weight`

```css
h1 {
	font-weight: bold;
}
```

- `font-weight` - толщина шрифта
	- Значения: `normal`, `bold`, `lighter`, `bolder`, `100-900` и тд.

### 🔸 `text-decoration`

```css
a { 
	text-decoration: none;
}
```

- `text-decoration` - управляет подчеркиванием, зачёркиванием и тд.
    - `underline` - подчеркивание (по умолчанию у ссылок, тег `<a>`)
    - `none` - без подчеркивания
    - `overline` - линия сверху
    - `line-through` - зачеркнутый текст

---
