### 🔹 Ссылки (`<a>`)

- **Примеры:**

```html
<a href="#">Пустая ссылка</a>
<a href="about.html">О нас</a>
<a href="https://google.com">Google</a>
```

- `<a>` - создаёт ссылку

- `href` - адрес перехода
#### 💡 Если указать `href="#"`, то:

- Ссылка **никуда не ведёт**, просто "заглушка". При клике URL меняется на #, но страница не перезагружается (часто используется при разработке или тестировании)

#### 🔹Атрибут `target`:

```html
<a href="page.html" target="_self">Ссылка</a>

<a href="https://example.com" target="_blank">Ссылка</a>
```

- `target` - определяет, где откроется ссылка при клике

- `_self` - открывает ссылку в той же вкладке

- `_blank` - открывает ссылку в новой вкладке

#### 🔹Использование атрибута `rel="noopener noreferrer"` с атрибутом `target="_blank"`:

```html
<!-- используется почти всегда с target="_blank" на внешние ссылки -->
<a href="https://example.com" target="_blank" rel="noopener noreferrer">Link</a>
```

- **`noopener`** – новая вкладка **не может управлять нашей страницей (т.е. не может получить к ней доступ через `window.opener`)** (безопасность!)

- **`noreferrer`** – **не передаёт адрес нашей страницы** на новый сайт