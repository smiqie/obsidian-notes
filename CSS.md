- Комментарии:
```css
/* Однострочный комментарий в CSS */

/* Многострочный
   комментарий
   в CSS */
```





### 🔹Стилизация списков через CSS (для `<ul>` и `<ol>`):

```css
ul {
	list-style-type: circle; 
}
ol {
	list-style-type: none; /* убирает точки у всех пунктов*/
}
```

- `disc` - чёрная точка

- `circle` - пустая точка

- `square` - квадрат

- `none` - без маркеров
