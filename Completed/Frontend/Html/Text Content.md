# `h1–h6` - заголовки

```html
<h1>Новости</h1>
  <h2>Политика</h2>
  <h2>Технологии</h2>
    <h3>ИИ и разработки</h3>
```

- `h1` - основной заголовок страницы, может быть только один (важен для SEO)
- `h2-h6` - структура контента, образуют иерархию (подзаголовки, разделы)
	- `h2` обычно используется внутри `<section>`
- 💡 Когда после заголовка идёт подзаголовок/описание - лучше обернуть оба в `<span>` внутри одного тега заголовка, чтобы поисковики учитывали оба текста:

```html
<!-- так себе -->
<h1>Туры на воздушном шаре</h1>
<p>Незабываемое приключение ждёт вас</p>

<!-- лучше — оба текста учитываются поисковиками -->
<h1>
  <span>Туры на воздушном шаре</span>
  <span>Незабываемое приключение ждёт вас</span>
</h1>
```

---

# `p` - абзац

```html
<p>Это абзац текста.</p>
```

- В `<p>` принято оборачивать минимум одно предложение
- **Особенность:** длинные строки без пробелов не переносятся и вылезают за границы контейнера:

```html
<p>Loremipsumdolorsitametconsectetursdgjdjsfdsgfshdgfhdgsfhdsgfhsdgfhsgdfsgdj</p>
```

---

# `strong` - важный текст

```html
<strong>Важный текст</strong>
```

- Семантическое выделение важности ✅ a11y - скринридеры подчёркивают интонацией
- Можно вкладывать для усиления важности

---

# `em` - акцент

```html
<em>Акцентированный текст</em>
```

- Семантический акцент ✅ a11y - скринридеры меняют интонацию

---

# `mark` - выделение

```html
<mark>Выделенный текст</mark>
```

- Выделяет текст жёлтым фоном по умолчанию ✅ a11y
- Используется для текста, важного в текущем контексте (например, совпадения в результатах поиска)

---

# `sub`, `sup` - подстрочный и надстрочный текст

```html
H<sub>2</sub>O   <!-- подстрочный: химические формулы -->
x<sup>2</sup>    <!-- надстрочный: степени, сноски -->
```

---

# `br`, `hr`

```html
Первая строка<br>Вторая строка
<hr>
```

- `<br>` - принудительный перенос строки
	- избегать в CSS-вёрстке, но допустимо для адресов и стихов
- `<hr>` - тематический разрыв (логический разделитель секций)
	- не использовать как декоративный элемент

---

# `code`, `pre` - код

```html
<code>const x = 10;</code>

<pre>
function hello() {
  console.log('Hi');
}
</pre>
```

- `<code>` - инлайн-код (моноширинный шрифт)
- `<pre>` - предформатированный текст (сохраняет пробелы и переносы)
	- часто используется с `<code>` внутри

---

# `blockquote`, `q`, `cite` - цитаты

```html
<!-- Блочная цитата -->
<blockquote cite="https://example.com/article">
  <p>Это длинная цитата, которая занимает несколько строк текста.</p>
</blockquote>
<cite>Уильям Шекспир, «Гамлет»</cite>

<!-- Строчная цитата -->
<q cite="https://example.com">Короткая цитата в строке</q>

<!-- Название источника внутри текста -->
<p>Как писал в своей статье <cite>«Искусство программирования»</cite>...</p>
```

- `<blockquote>` - блочный элемент для больших цитат ✅ a11y
	- Атрибут `cite` - URL источника (не отображается визуально) ✅ a11y
- `<q>` - строчный элемент для коротких цитат внутри текста ✅ a11y
	- Атрибут `cite` - URL источника (не отображается визуально) ✅ a11y
- `<cite>` - строчный тег для источника цитаты ✅ a11y
	- идёт сразу после `<blockquote>` или `<q>`
	- **Важно:** `<cite>` - это источник (книга, статья, сайт), а не имя автора
	- Цитата без указания источника - плохой тон
	- `<cite>` также используется для названий объектов искусства: книг, статей, картин, фильмов