## `ul` - ненумерованный список

```html
<ul>
  <li>Первый пункт</li>
  <li>Второй пункт</li>
</ul>
```

- `<ul>` - визуальная обёртка маркированного списка
- `<li>` - элемент списка (пункт)
- 💡 `<ul>` принято использовать не только для текстовых списков, но и для **любой упорядоченной последовательности**: меню, карточки, кнопки, навигация, шаги рецепта и т.д.

---

## `ol` - нумерованный список

```html
<ol>
  <li>Первый пункт</li>
  <li>Второй пункт</li>
</ol>
```

- `<ol>` - обёртка нумерованного списка
- Встречается реже, чем `<ul>`