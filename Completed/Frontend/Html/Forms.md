# `form` - форма

```html
<form action="/submit" method="post">
  <fieldset>
    <legend>Личные данные</legend>

    <label for="name">Имя:</label>
    <input type="text" id="name" name="user-name" required>

    <label for="email">Email:</label>
    <input type="email" id="email" name="user-email" required>
  </fieldset>

  <button type="submit">Отправить</button>
</form>
```

- `<form>` - обёртка элементов, с которыми взаимодействует пользователь ✅ a11y
- **Атрибуты:**
    - `action` - адрес серверного скрипта-обработчика
    - `method` - метод отправки: `get` (по умолчанию) / `post`
    - `novalidate` - отключает стандартную валидацию (используется при валидации через JS)

---

# `fieldset` + `legend` - группировка полей

```html
<fieldset>
  <legend>Контактная информация</legend>
  <!-- поля формы -->
</fieldset>
```

- `<fieldset>` - визуальная группировка нескольких полей в один набор ✅ a11y
- `<legend>` - заголовок внутри `<fieldset>` ✅ a11y

---

# `label` - метка поля

```html
<label for="login">Логин:</label>
<input type="text" id="login" name="user-login">
```

- Метка к интерактивному элементу ✅ a11y (клик по тексту = фокус на поле ввода)
- **Атрибут:**
    - `for` - связка с `id` элемента формы

---

# `input` - поле ввода

```html
<!-- Текстовые поля -->
<input type="text" id="name" name="user-name" value="Иван">
<input type="email" id="email" name="user-email">      <!-- ✅ валидация, 📱 спец. клавиатура -->
<input type="url" name="website">                      <!-- ✅ валидация, 📱 спец. клавиатура -->
<input type="tel" name="phone">                        <!-- 📱 спец. клавиатура -->
<input type="search" name="query">                     <!-- 📱 спец. клавиатура -->
<input type="number" name="age" min="18" max="100">    <!-- ✅ валидация, 📱 спец. клавиатура -->
<input type="password" name="pass">

<!-- Дата и время -->
<input type="date" name="birthday">
<input type="datetime-local" name="meeting">
<input type="month" name="period">
<input type="week" name="week-num">
<input type="time" name="alarm">

<!-- Специальные -->
<input type="color" name="theme">
<input type="file" name="avatar" multiple>
<input type="range" name="volume" min="0" max="100">

<!-- Чекбоксы и радио -->
<input type="checkbox" id="agree" name="terms" value="agreed" checked>
<label for="agree">Согласен с условиями</label>

<input type="radio" id="male" name="gender" value="male" checked>
<label for="male">Мужской</label>
<input type="radio" id="female" name="gender" value="female">
<label for="female">Женский</label>
```

- **Обязательные атрибуты:**
    - `id` - связка с `<label>` через `for`
    - `name` - имя поля (сервер распознаёт данные по `name`)
    - `value` - значение по умолчанию в поле ввода / значение для сервера (обязательно для `checkbox` и `radio`)
- **Типы (`type`):**
    - `text` - обычное текстовое поле (по умолчанию)
    - `email` - ✅ валидация формата, 📱 спец. клавиатура
    - `url` - ✅ валидация формата, 📱 спец. клавиатура
    - `tel` - 📱 спец. клавиатура (без валидации)
    - `search` - 📱 спец. клавиатура для поиска
    - `number` - ✅ только числа, 📱 спец. клавиатура
    - `password` - скрывает вводимые символы
    - `date` / `datetime-local` / `month` / `week` / `time` - выбор даты/времени
    - `color` - встроенный color picker
    - `file` - загрузка файлов (`multiple` - несколько файлов)
    - `checkbox` - чекбокс (`checked` - выбран по умолчанию)
    - `radio` - выбор одного варианта из группы (общий `name` для группы, `checked` - выбран по умолчанию)
    - `range` - ползунок (`min`, `max` для диапазона)

---

# `textarea` - многострочное поле ввода

```html
<textarea id="comment" name="user-comment" rows="5" cols="30">
Текст по умолчанию
</textarea>
```

- **Атрибуты:**
    - `id`, `name` - стандартные
    - `rows`, `cols` - высота/ширина в строках/символах (на практике настраивается через CSS)
    - `wrap` - форматирование текста при отправке:
        - `soft` - переносы не изменяются (по умолчанию)
        - `hard` - добавляет символы переноса строк

---

# `select` + `option` + `optgroup` - выпадающий список

```html
<!-- Базовый -->
<select id="city" name="user-city" required>
  <option value="moscow">Москва</option>
  <option value="spb">Санкт-Петербург</option>
</select>

<!-- С placeholder -->
<select id="city" name="user-city">
  <option value="" disabled selected>Не выбрано</option>
  <option value="moscow">Москва</option>
  <option value="spb">Санкт-Петербург</option>
</select>

<!-- С группировкой -->
<select id="country" name="user-country" multiple>
  <optgroup label="Европа">
    <option value="russia">Россия</option>
    <option value="germany">Германия</option>
  </optgroup>
  <optgroup label="Азия">
    <option value="japan">Япония</option>
  </optgroup>
</select>
```

- `<select>` - выпадающий список ✅ a11y
	- **Атрибуты `<select>`:** `id`, `name`, `required`, `multiple` (множественный выбор - список не выпадающий)
- `<option>` - вариант списка
	- Атрибуты: `value` (значение для сервера, обязательно), `selected` (выбран по умолчанию), `disabled` (недоступен для выбора)
- `<optgroup>` - визуальная группировка вариантов (встречается редко)
	- Атрибут `label` - название группы
- **Кастомизация:** чаще используют готовые библиотеки, т.к. стилизовать через CSS сложно

---

# Атрибуты форм

```html
<!-- Валидация -->
<input type="text" required>
<input type="text" minlength="3" maxlength="20">
<input type="text" pattern="[a-zA-Z]+">

<!-- Состояние -->
<input type="text" disabled>   <!-- неактивно, данные не отправляются -->
<input type="text" readonly>   <!-- только чтение, данные отправляются -->

<!-- Подсказки -->
<input type="text" placeholder="Введите имя">
<input type="text" autocomplete="on">
<input type="text" autofocus>
<input type="text" spellcheck="true">

<!-- Мобильные клавиатуры -->
<input type="text" inputmode="numeric">
<input type="text" inputmode="email">

<!-- Связка с формой -->
<input type="text" form="user-info">
<form id="user-info"></form>
```

- `required` - обязательное поле (`input`, `textarea`, `select`)
- `disabled` - элемент неактивен, данные не отправляются (`input`, `textarea`, `select`, `button`, `fieldset`, `option`, `optgroup`)
- `readonly` - только чтение, можно фокусироваться, данные отправляются (`input`, `textarea`)
- `placeholder` - текст-подсказка внутри поля (`input`, `textarea`)
- `inputmode` - 📱 альтернативная клавиатура (`text`, `numeric`, `decimal`, `tel`, `email`, `url`, `search`, `none`). Используй сначала правильный `type`, затем `inputmode` для уточнения
- `autocomplete` - автозаполнение (`on` / `off`)
- `autofocus` - автофокус при загрузке страницы
- `form` - связка элемента с формой вне её (полезно при сложном дизайне)
- `minlength` / `maxlength` - ограничение символов (`input`, `textarea`)
- `pattern` - регулярное выражение для валидации (`input`)
- `spellcheck` - проверка орфографии (`true` / `false`)