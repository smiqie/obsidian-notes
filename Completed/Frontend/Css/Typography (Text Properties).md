# `text-align` - выравнивание текста

```css
text-align: left;
text-align: right;
text-align: center;
text-align: justify;
```

- выравнивает текст внутри блока (работает только с блочными и `inline-block` элементами)
- **Значения:**
    - `left` - по левому краю (по умолчанию)
    - `right` - по правому краю
    - `center` - по центру
    - `justify` - по ширине (добавляет пробелы между словами для заполнения всей ширины)

---

# `font-size` - размер шрифта

```css
font-size: 16px;
font-size: 100%;
font-size: large;
```

- задаёт размер шрифта

---

# `font-weight` - жирность шрифта

```css
font-weight: 100; /* Thin */
font-weight: 200; /* Extra Light */
font-weight: 300; /* Light */
font-weight: 400; /* Normal / Regular */
font-weight: 500; /* Medium */
font-weight: 600; /* Semi Bold */
font-weight: 700; /* Bold */
font-weight: 800; /* Extra Bold */
font-weight: 900; /* Black */
```

- задаёт толщину (жирность) шрифта
	- **обычно указывают в числах**

---

# `font-style` - начертание шрифта

```css
font-style: normal;
font-style: italic;
font-style: oblique;
```

- задаёт начертание шрифта
- **Значения:**
    - `normal` - обычный шрифт (по умолчанию)
    - `italic` - курсив (специальный курсивный вариант шрифта)
    - `oblique` - наклонный (искусственный наклон обычного шрифта, если нет `italic`)

---

# `font-family` - семейство шрифта

```css
/* конкретный шрифт */
font-family: 'Arial';

/* несколько шрифтов (fallback) */
font-family: 'Roboto', 'Arial', sans-serif;

/* общие типы шрифтов */
font-family: serif;      /* с засечками */
font-family: sans-serif; /* без засечек (встречается чаще) */
font-family: monospace;  /* моноширинный */
font-family: cursive;    /* рукописный */
font-family: fantasy;    /* декоративный */
```

- задаёт семейство шрифта (если шрифт состоит из нескольких слов - оборачивать в кавычки)
- **Fallback-механизм:** если первый шрифт недоступен, браузер использует следующий
- 💡 Последним всегда указывать общий тип: `serif`, `sans-serif`, `monospace`

---

# `font` - шорткат

```css
/* style weight size/line-height family */
font: italic 700 16px/1.5 'Arial', sans-serif;

/* минимальная запись */
font: 16px 'Arial', sans-serif;

/* с line-height */
font: 16px/1.5 sans-serif;
```

- сокращённая запись для всех `font`-свойств
- **Паттерн:** `font: [style] [weight] size[/line-height] family;`
	- `size` и `family` - обязательные

---

# `text-decoration` - декоративные линии текста

```css
text-decoration: none;
text-decoration: underline;
text-decoration: overline;
text-decoration: line-through;

/* несколько линий одновременно */
text-decoration: underline overline;

/* с цветом и стилем */
text-decoration: underline wavy red;
```

- управляет декоративными линиями текста
- **Значения:**
    - `none` - без декорации (часто используется чтобы убрать подчёркивание у ссылок)
    - `underline` - подчёркивание
    - `overline` - линия сверху
    - `line-through` - зачёркивание
- Можно комбинировать с цветом и стилем линии: `solid`, `dashed`, `dotted`, `wavy`

---

# `line-height` - межстрочный интервал

```css
line-height: 1.5;    /* рекомендуется */
line-height: 24px;
line-height: 150%;
line-height: normal; /* ~1.2, по умолчанию */

/* пример */
p {
  font-size: 16px;
  line-height: 1.5; /* высота строки = 24px (16 × 1.5) */
}
```

- задаёт высоту строки текста
- **Значения:**
    - коэффициент (`1.5`) - множитель от `font-size` (рекомендуется)
    - `px`, `em`, `rem` - фиксированная высота
    - `%` - процент от `font-size`
    - `normal` - браузерное значение (~1.2)
- 💡 **Лучшая практика:** использовать числа без единиц (`1.5`), чтобы значение наследовалось корректно

---

# `letter-spacing` - межбуквенный интервал

```css
h1 {
  letter-spacing: 3px;
}
```

- задаёт расстояние между символами
- **Значения:**
    - `normal` - стандартное расстояние (по умолчанию)
    - `px`, `em`, `rem` - любые единицы измерения
    - можно использовать отрицательные значения для сжатия текста

---

# `text-transform` - регистр текста

```css
text-transform: none;
text-transform: uppercase;
text-transform: lowercase;
text-transform: capitalize;
```

- **Значения:**
    - `none` - без изменений (по умолчанию)
    - `uppercase` - все буквы в верхний регистр
    - `lowercase` - все буквы в нижний регистр
    - `capitalize` - первая буква каждого слова в верхний регистр
- 💡 **Зачем `uppercase` если можно написать капсом в разметке?** Разделение ответственности: HTML хранит контент, CSS управляет отображением. Нужно убрать капс - меняешь одну строку CSS, а не трогаешь разметку