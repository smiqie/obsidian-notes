# `list-style` - шорткат для маркеров списка

```css
/* шорткат <type> <position> <image> */
list-style: disc inside url('marker.png');
```

- шорткат для `list-style-type`, `list-style-position`, `list-style-image`
- **Паттерн:** `list-style: <type> <position> <image>`

---

# `list-style-type` - маркеры списка

```css
list-style-type: none;

/* для <ul> */
list-style-type: disc;   /* закрашенный круг (по умолчанию) */
list-style-type: circle; /* пустой круг */
list-style-type: square; /* квадрат */

/* для <ol> */
list-style-type: decimal;              /* 1, 2, 3... (по умолчанию) */
list-style-type: decimal-leading-zero; /* 01, 02, 03... */
list-style-type: lower-roman;          /* i, ii, iii... */
list-style-type: upper-roman;          /* I, II, III... */
list-style-type: lower-alpha;          /* a, b, c... */
list-style-type: upper-alpha;          /* A, B, C... */
```

- задаёт вид маркера для списков (`<ul>`, `<ol>`)
- **Значения для `<ul>`:** `disc` (по умолчанию), `circle`, `square`
	- `none` - убрать маркеры
- **Значения для `<ol>`:** `decimal` (по умолчанию), `lower-alpha` / `upper-alpha`, `lower-roman` / `upper-roman`
	- `none` - убрать маркеры
- 💡 На практике часто используют `none` и делают кастомные маркеры через CSS

---

# `list-style-position` - положение маркера

```css
list-style-position: outside; /* по умолчанию */
list-style-position: inside;
```

- **Значения:**
    - `outside` - маркер снаружи блока текста (по умолчанию)
    - `inside` - маркер внутри, текст обтекает его

---

# `list-style-image` - маркер-изображение

```css
list-style-image: none; /* по умолчанию */
list-style-image: url('marker.png');
```

- заменяет стандартный маркер на изображение
- 💡 На практике редко используют - проще сделать кастомный маркер через `background-image` или псевдоэлемент `::before`

---

# `border-collapse` - границы ячеек таблицы

```css
border-collapse: separate; /* по умолчанию */
border-collapse: collapse;
```

- управляет отображением границ ячеек таблицы (работает только с `<table>`)
- **Значения:**
    - `separate` - границы ячеек раздельные (по умолчанию)
    - `collapse` - границы ячеек объединяются в одну линию
- 💡 Почти всегда используется `collapse`, чтобы убрать двойные границы