# `object-fit` - заполнение контейнера медиаэлементом

```css
object-fit: cover;
object-fit: contain;
object-fit: fill;
object-fit: none;
object-fit: scale-down;

/* пример */
.card-image {
  width: 300px;
  height: 200px;
  object-fit: cover; /* изображение заполнит блок, сохраняя пропорции */
}
```

- управляет тем, как **изображение или видео** заполняет контейнер с фиксированными размерами (работает только с `<img>` и `<video>`)
- **Значения:**
    - `cover` - заполняет контейнер, сохраняя пропорции (края могут обрезаться)
	    - идеально для превью и карточек
    - `contain` - полностью помещается в контейнер, сохраняя пропорции (могут быть пустые места по краям)
	    - важно показать все изображение
    - `fill` - растягивается на весь контейнер (пропорции могут нарушиться) (по умолчанию)
    - `none` - оригинальный размер без масштабирования (может выходить за границы)
    - `scale-down` - выбирает меньший результат между `none` и `contain`
- 💡 Чаще всего используется `cover` - заполняет контейнер без искажений