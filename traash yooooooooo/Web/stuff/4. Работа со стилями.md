
```

- **User Agent Stylesheet** - встроенные браузером стили по умолчанию. **Приориет:** 4 (самый низкий)
```css
/* нпр., браузер сам делает тег <p> черным и с отступами, если не задано CSS */
```

--- 
## 🔹 Подключение стилей

#### 1️⃣ Через `<link>` (внешний файл)

```html
<link rel="stylesheet" href="style.css">
```

- `rel="stylesheet"` - говорит, что это таблица стилей
- `href="style.css"` -  путь к файлу `style.css`

💡 **Файл `.CSS` не обязательно должен лежать в одной папке с HTML**:

  ```html
  <!-- путь правильный! относительно места, где лежит HTML файл-->
  <link rel="stylesheet" href="../css/style.css">
  ```
  
  -  `../` - переход на уровень выше в файловой структуре
#### 2️⃣ Через `<style>` (встроенные стили)

```html
<head>
    <style>
        p { color: blue; }
        .box-1 { background-color: red; }
    </style>
</head>
```

- Обычно тег `<style>` пишут в теге `<head>`, т.к. браузер читает код сверху вниз, стили применяются до отображения элементов

```
<link rel="icon" href="icon.png"> 
подключение фав-иконки
```