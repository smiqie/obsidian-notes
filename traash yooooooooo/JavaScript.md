## 1. Объявление переменных

### Типы объявлений

```js
var x = 10;    // ❌ устаревшее, глобальная видимость
let y = 20;    // ✅ локальная, можно менять
const z = 30;  // ✅ локальная, нельзя менять ссылку
```

### Хорошие и плохие практики

```js
// ❌ Плохо - неудобно читать
const name='Max', age=28, isDeveloper=true;

// ✅ Хорошо - каждая переменная на отдельной строке
const name = 'Max';
const age = 28;
const isDeveloper = true;
```

### Строгий режим

```js
// ❌ Плохо - создаётся глобальная переменная
age = 10;

// ✅ Хорошо - используйте строгий режим
'use strict'; // прописывать в начале каждого файла JS
```

**Строгий режим** запрещает создавать переменные без `let`/`const`/`var`

---

## 2. Правила нейминга

**camelCase** - для переменных и функций

```js
let isEnabled = true;
```

**PascalCase** - для классов и конструкторов

```js
class UserAccount {}
```

**SCREAMING_SNAKE_CASE** - для констант

```js
const API_KEY = '123456';
```

---

## 3. Типы данных

### Примитивные типы

|Тип|Пример|Комментарий|
|---|---|---|
|`number`|`10`, `3.14`|целые и дробные числа|
|`string`|`'Hello'`|текст|
|`boolean`|`true / false`|логические значения|
|`undefined`|`undefined`|переменная без значения (неинициализирована)|
|`bigint`|`9007199254740991n`|большие числа, > `Number.MAX_SAFE_INTEGER`|
|`symbol`|`Symbol('id')`|уникальные и неизменяемые идентификаторы|

### Специальные и объектные типы

|Тип|Пример|Комментарий|
|---|---|---|
|`null`|`null`|явно пустое значение|
|`object`|`{a:1}` / `[1,2,3]` / `function(){}`|объекты, массивы, функции|

---

## 4. Особенности типов данных

### String (строки)

**Кавычки:**

```js
'text'                  // одинарные
"text"                  // двойные
`text ${variable}`      // backticks (обратные кавычки) - позволяют интерполяцию
```

**Интерполяция:**

```js
const name = 'Max';
const a = 5, b = 3;
const msg = `name: ${name}, sum: ${a + b}`; // ✅ читабельно

// ❌ Плохой тон - конкатенация через +
const sum = "sum: " + (a + b);
```

### Number (числа)

**Особенности:**

```js
100 / 0      // Infinity
-100 / 0     // -Infinity
'Word' / 100 // NaN
```

**Диапазон безопасных целых чисел:**

- `Number.MIN_SAFE_INTEGER = -9007199254740991`
- `Number.MAX_SAFE_INTEGER = 9007199254740991`

**Читаемость больших чисел:**

```js
const num = 1_000_000; // ✅ удобно читать
```

### BigInt (большие целые)

Для чисел больше `Number.MAX_SAFE_INTEGER`:

```js
const big = 900n; // ✅

// Операции только с другими bigint
2n + 1;   // ❌ ошибка
2n + 1n;  // ✅ работает
```

### Null (пустое значение)

```js
let data = null; // ✅ переменная пока пустая, ждём значение
```

- Обычно присваивают переменным, значение которых ещё не получено (нпр., с сервера), но вскоре ожидается

### Undefined (неопределённое)

```js
let x; // x === undefined ✅

// ❌ Плохой тон
let name = undefined;

// ✅ Хорошо
let name = null;
```

---

## 5. Работа с типами

### Оператор typeof

Возвращает строку с типом переменной:

```js
const name = 'Max';
console.log(typeof name); // "string"
```

### Преобразования типов

#### ❌ Неявные (плохой тон)

JS сам пытается привести типы, часто неожиданно:

```js
const num = 111;
const str = '222';
console.log(num + str);  // "111222" - приводит число к строке
console.log('16' / '8'); // 2 - приводит строки к числу
```

#### ✅ Явные (хорошая практика)

Используем глобальные функции:

```js
Number('16');   // конвертирует в number
String(123);    // конвертирует в string
Boolean(0);     // конвертирует в boolean
                // false при: '', null, 0, undefined, NaN
                // true для всего остального
```

---

## 6. Операторы

### Арифметические операторы

`+`, `-`, `/`, `*`, `%`, `**`, инкремент, декремент

**Постфиксный инкремент/декремент (`a++` / `a--`):**

```js
let a = 10;
let b = a++;
// a = 11
// b = 10
```

**Префиксный инкремент/декремент (`++a` / `--a`):**

```js
let a = 10;
let b = ++a;
// a = 11
// b = 11
```

### Операторы сравнения

`==`, `===`, `!=`, `!==`, `>`, `<`, `<=`, `>=`

**Строгое сравнение (рекомендуется):**

```js
2 === '2' // false (типы не совпадают)
2 !== 2   // false

// ❌ Избегайте == и != (приводят типы)
// ✅ Используйте === и !== (учитывают тип)
```

**Сравнение строк:**

⚠️ Строки сравниваются по порядку символов в Unicode

```js
'JavaS' > 'Java'  // true (длина + сравнение позиционно)
'ы' > 'f'         // true (т.к. кириллица идёт позже латиницы в юникоде)
```

### Тернарный оператор

```js
const age = 18;
const status = age >= 18 ? 'Adult' : 'Minor';

// Вложенный тернарный оператор
const status = age >= 18
    ? 'Adult'
    : age < 18
        ? 'Child'
        : 'Nothing'
```

**Синтаксис:** `условие ? значение1 : значение2`

### Операторы присваивания

`=`, `+=`, `-=`, `/=`, `*=`, `%=`, `**=`

**Для строк `+=` это конкатенация:**

```js
let str = "Hello";
str += " world"; // "Hello world"
```

---

## 7. Классификация операторов

**Унарный оператор** - работает с одним операндом

```js
-x, +x, !x, typeof x, ++x, x--
```

**Бинарный оператор** - работает с двумя операндами

```js
a + b, a - b, a > b, a && b
```

**Тернарный оператор** - три операнда

```js
условие ? выражение1 : выражение2
```

---

## 8. Унарные плюс и минус

### Унарный минус (-)

Делает число отрицательным:

```js
const a = 10;
console.log(-a); // -10
```

### Унарный плюс (+)

Преобразует значение к числу:

```js
+'123'       // 123
+'3.14'      // 3.14
+true        // 1
+false       // 0
+'abc'       // NaN
```

То же самое, что `Number('123')`.

---

## 9. Логические операторы

### Оператор ИЛИ (`||`)

Возвращает **первое истинное значение**.

```js
const a = '' || null || false || 0 || 'Hi';
console.log(a); // 'Hi'
```

### Оператор И (`&&`)

Возвращает **первое ложное значение** или последний операнд, если все истинные.

```js
'Hi' && true && 5 && null    // null
1 && 2 && 3                  // 3
```

**⚠️ Частая, но не рекомендуемая практика:**

```js
// ❌ Лучше использовать if
const age = 18;
const isYoung = age < 18;
isYoung && console.log('Close'); // если isYoung true, выполнится инструкция после &&
```

### Оператор НЕ (`!`)

**Двойное отрицание:**

```js
!!0 === Boolean(0)   // true
```

👉 **Но лучше использовать `Boolean()` - читабельнее**

### Приоритет операторов

`&&` имеет **приоритет выше**, чем `||`

```js
true || false && false
// то же самое, что true || (false && false)
```

---

## 10. Оператор нулевого слияния (`??`)

Возвращает **первое значение, которое НЕ null/undefined**.

```js
const a = null;
const b = 100;

const res = a ?? b; // 100
```

⚠️ `??` срабатывает **только** на `null` и `undefined`

---

## 11. Условные конструкции

### If/Else

```js
if (условие) {
    // код
} else if (другое_условие) {
    // код
} else {
    // код
}
```

**❌ Плохой код (хуже читается):**

```js
if (условие)
    // одна строка кода без фигурных скобок
```

**✅ Хорошо - всегда используйте фигурные скобки**

---
## 12. Циклы

### While

```js
while (условие) {
    // код
}
```

⚠️ Используется редко из-за риска утечки памяти (можно задать неверное условие выхода)

### Do While

```js
do {
    // код
} while (условие);
```

### For

```js
for (let i = 0; i < 5; i++) {
    // код
}

// Можно делать так, но нет смысла (т.к. есть while)
let i = 0;
for (; i < 4; ) {
    i++;
}
```

### Ключевые слова

- `break` - выход из цикла
- `continue` - переход к следующей итерации

---

## 13. Switch Case

### Простой пример

```js
const day = 3;

switch (day) {
    case 1:
        console.log('Понедельник');
        break;
    case 2:
        console.log('Вторник');
        break;
    case 3:
        console.log('Среда');
        break;
    default:
        console.log('Другой день');
}
```

### Проверка диапазона

```js
const score = 85;

switch (true) {
    case score >= 90:
        console.log('Отлично');
        break;
    case score >= 70:
        console.log('Хорошо');
        break;
    case score >= 50:
        console.log('Удовлетворительно');
        break;
    default:
        console.log('Неудовлетворительно');
}
```

---
## 14. Alert, Prompt, Confirm

⚠️ Редко используются на практике

### Alert

- Показывает модальное окно с сообщением
- Останавливает код до нажатия ОК

```js
alert('Привет!');
```

**Параметры:**

- `message` - текст сообщения

---

### Prompt

- Показывает окно с полем ввода
- Возвращает `string` с введённой строкой, или `null` если ничего не введено
- Останавливает код до нажатия ОК/Отмена

```js
const name = prompt('Введите имя:');
const age = +prompt('Введите возраст:', '18'); // со значением по умолчанию
```

**Параметры:**

- `message` - текст вопроса
- `default` - значение по умолчанию (необязательно)

---

### Confirm

- Показывает окно с кнопками ОК/Отмена
- Возвращает `true` если нажали ОК или `false` если нажали Отмена
- Останавливает код до нажатия ОК/Отмена

```js
const isConfirmed = confirm('Вы уверены?');

if (isConfirmed) {
    console.log('Подтверждено');
} else {
    console.log('Отменено');
}
```

**Параметры:**

- `message` - текст вопроса

---

## 15. Console.log

Выводит данные в консоль браузера. Используется для отладки

```js
console.log('текст'); // пример
```

---
## 16. Function Declaration

```js
function greet(name) {
    return `Hello, ${name}!`;
}

greet('Max'); // можно вызвать до объявления
```

**Особенности:**

- ✅ Можно использовать до объявления (hoisting - поднятие)
- ✅ Можно перезаписать
- ✅ Доступ к неявной переменной `arguments`
- ✅ Свой контекст `this`

**Hoisting:**

```js
sayHi(); // ✅ работает

function sayHi() {
    console.log('Hi!');
}
```

JS при обработке кода поднимает все `function` наверх их области видимости, как будто они объявлены в начале файла.

**Arguments:**

```js
function sum() {
    console.log(arguments); // [1, 2, 3]
    return arguments[0] + arguments[1];
}

sum(1, 2, 3);
```

---

## 17. Function Expression

Функция записывается как выражение и присваивается переменной.

```js
const greet = function(name) {
    return `Hello, ${name}!`;
};

greet('Max');
```

**Особенности:**

- ❌ Нельзя использовать до объявления (код становится очевиднее)
- ❌ Нельзя перезаписать (если используется `const`)
- ✅ Доступ к `arguments`
- ✅ Свой контекст `this`

**Использование до объявления:**

```js
greet('Max'); // ❌ ReferenceError

const greet = function(name) {
    return `Hello, ${name}!`;
};
```

**Перезапись:**

```js
const greet = function() { return 'Hi'; };
greet = function() { return 'Hello'; }; // ❌ TypeError с const

let greet2 = function() { return 'Hi'; };
greet2 = function() { return 'Hello'; }; // ✅ работает с let
```

---

## 18. Arrow Functions (современный подход)

```js
const greet = (name) => {
    return `Hello, ${name}!`;
};

// или короче
const greet = (name) => `Hello, ${name}!`;
```

**Особенности:**

- ❌ Нельзя использовать до объявления
- ❌ Нет доступа к `arguments`
- ❌ Нет своего `this` (берёт из родительской области)
- ✅ Неявный возврат (если одна строка)

**Неявный возврат:**

```js
const sum = (a, b) => a + b; // без return и {}

const getUser = () => ({ name: 'Max', age: 28 }); // для объектов нужны ()
```

**Копирование функций:**

```js
const fn1 = () => 'text';
const fn2 = fn1; // копирование по ссылке
```

**Функции-колбеки:**

```js
const msg = (action1, action2) => {
    action1();
    action2();
};

msg(
    () => console.log('1'),
    () => console.log('2')
);

// или с промежуточными переменными
const fn1 = () => console.log('1');
const fn2 = () => console.log('2');
msg(fn1, fn2);
```

**Функция возвращает функцию:**

```js
const validate = (hasAccess) => 
    hasAccess 
        ? () => console.log('Доступ разрешён')
        : () => console.log('Доступ запрещён');

const logMsg = validate(false);
logMsg(); // "Доступ запрещён"
```

---

## 19. Лучшие практики работы с функциями

- Функция должна выполнять только одну задачу

- Имя начинается с **глагола** + уточняющее **существительное** (нпр., validateEmail, isActive, calculateTotal и тд.)

- При беглом прочтении кода достаточно прочитать имя функции, чтобы понять что она делает.

```js
// ✅ Понятно без чтения тела функции
checkUserPermissions();
fetchDataFromAPI();
renderUserProfile();
```

---

## 20. Параметры функций

### Параметры и аргументы

```js
function greet(name, age) { // name, age - параметры
    console.log(name, age);
}

greet('Max', 28); // 'Max', 28 - аргументы
```

### Параметры по умолчанию

Параметры по умолчанию принято указывать **в конце**.

```js
// ✅ Хорошо
function greet(name, greeting = 'Hello') {
    return `${greeting}, ${name}!`;
}

greet('Max'); // "Hello, Max!"
greet('Max', 'Hi'); // "Hi, Max!"
```

### Проверка параметров

Проверка на `null`, `undefined`, пустую строку и другие falsy значения:

```js
function processData(data) {
    if (!data) {
        console.log('Нет данных');
        return;
    }
    // обработка данных
}
```