### **Относительные пути** - ссылаются **относительно текущего файла**:

```html
<!-- Путь от текущей папки (лучше читается) -->
<img src="./img/example.png" alt="">

<!-- То же самое, но без "./" (менее явно) -->
<img src="img/example.png" alt="">


<!-- Выход на уровень выше -->
<img src="../img/example.png" alt="">
```

### **Абсолютные пути** - указывают **относительно корня сайта** (корневой папки проекта):

```html
<!-- Всегда начинаются со слеша "/", путь считается от корня проекта -->
<img src="/img/example.png" alt="">
<a href="/pages/about.html">О нас</a>
```

### **Полные URL (внешние ресурсы)**

```html
<img src="https://example.com/img/photo.jpg" alt="">
<a href="https://google.com">Google</a>
```

---
## **Работа со шрифтами в CSS**

**Шрифт** - это набор символов (букв, цифр, знаков препинания) определённого начертания и стиля, который определяет визуальный облик текста на странице
### **Шрифты с засечками (Serif) или без (Sans-serif)**

- **Serif (с засечками)** - шрифты с небольшими "хвостиками" на концах букв  
    Используются: в печатных изданиях, книгах, для классического/формального стиля
    
- **Sans-serif (без засечек)** - шрифты с чистыми, ровными линиями  
    Используются: в веб-дизайне (лучше читаются на экранах), для современного/минималистичного стиля

---
## **Иконочные шрифты**

**Иконочные шрифты** - это шрифты, где вместо букв находятся иконки/символы (стрелки, логотипы, значки соцсетей и т.д.)

**Преимущество**: их можно редактировать как обычный текст через CSS-свойства:

```css
.icon {
  font-size: 24px;      /* размер */
  color: #ff0000;       /* цвет */
  font-weight: bold;    /* жирность */
}
```

---
# 📁 Структура проекта

#### Папка `src/` (source) - содержит все исходные материалы

- `css/` (стили)
- `js/` (скрипты)
- `fonts/` (шрифты)
- `img/` (изображения)
- `icons/` (иконки)
- `index.html` - главная страница (`index.html` - стандартное имя главной страницы, браузеры открывают её первой)
- `другие .html страницы`

- `dist/` (distribution) - готовая и оптимизированная версия сайта
-  `src` - черновик,  `dist` - финальная сборка сайта

💡 **Названия папок и файлов рекомендуется писать с маленькой буквы через дефис** (например: `my-site`, `main-page.html`), чтобы избежать проблем при загрузке на хостинг (некоторые серверы чувствительны к регистру)

💡Общепринятое правило: папка `img` - растровые изображения. папка `icons` - векторные изображения

---


