- массив - стркутура данных для хранения упорядоченных коллекций (нпр список пользователей, товаров, элементов)
- создение массива
```js
// 1
const arr = [];
// 2
const arr = new Array();
```
- объеклвение и иницализация массива?
```js
const numbers = [1, 10, 21];
/*под капотом это выглдяит как ключ знаечния:
0: 1
1: 10
2: 21
length: 3
*/


// получение элементов массива
numbers[0] // 1
numbers[2] // 21

// массив может быть с различными значениями
const arrayMix = [1, 2, "hello", true, undefined, null, {}, []];

// массив который выключает в себя массив (как элемент) - называется двумерным, пример:
const arrayMix = [[]];

// копирвоание значения элеемнта в массиве в пемренную
let x = numbers[1]; // x = 10

// переприсваивание элемента в массиве (изменить значение у элемента), даже если массив был обяълен как const (т.к. мы не меняем сам массив а меняем его элементы)
numbers[1] = 100; // numbers[1] = 100



----

или создание через класс аррей
const arr = new Array(1, 2, 34, true); // резульата будет такой же
```
- перечисление элементов массив анаичнаются с нулеовго индекса!
- свойства массива:
	1. `const numbers = [1, 2, 3]`
	2. `numbers.length` - возвращает кол-во элементов в массиве

- копирование массивов
```js
const numbers = new Array(10, 20 ,30);
const copiedArray = numbers;
console.log(numbers === copiedArray); // true (тк они обеъкты)
```

- методы массивов:
	1. `const arr = new Array(1, 2, 3, 4, 5);`
	2. `arr.unshift(111)` - добавляет новый элемент в начало массива, отасльные элементы смещаются
		- `(111, 1, 2, 3, 4, 5)`
	3. `arr.push(777)` - добавляет новый элемент в конец массива, одлин из самых попялрных методов - **более предпочтительней чем метод unshift, тк чтобы положить элемент в начало массива - более затртаная операция чем в конец**
		- `(1, 2, 3, 4, 5, 777)`
	4. `arr.shift()` - удаляет элемент из начала массива и соответственно он его еще и возвращает
		-  `(2, 3, 4, 5)`
	5. `arr.pop()` - удаляет элемент из конца массива // и соответственно он его еще и возвращает?
		-  `(1, 2, 3, 4)`
	6. `arr.sort((a, b) => a - b)` - сортирвока массива по возрастанию, принимает в себя колюэк функцию () - `5, 4, 3, 2, 1`
		1. `arr.sort((a, b) => b - a)` - сортирвока массива по убывнаию, принимает в себя колюэк функцию -  `1, 2, 3, 4, 5`
		   или `arr.sort((a, b) => a - b).reverse()` - `1, 2, 3, 4, 5`
		- часто используются в реальной разработке
	7. `arr.reverse()` - переворачивает массив `5, 4, 3, 2, 1` изменяет массив напрямую?? т.е. даже в перемнную не надо присваивать резальтат они просто меняют сам массив, как и прошлые метожды?? / также етсь метод `.toReversed()` который тожд не изменяет исходный массив а просто делает копию
	8. `arr.toSorted((a, b) => a - b)` - рпинимает такие же параметры как и метод sort но никак не мутирует наш массив (т.е. чтобы сохранить изнения его нужджно присовить??) !!этот метод считается новым!! - в данном примере сортировка от меньшего к большему / не изменяет искходный массив а просто делает его копию 
	9. `const newArr = arr.concat(arr1, arr2)` - используется для **объединения** двух или более массивов / **Немутируемость:** Исходные данные остаются прежними - т.е. он возвращает зщначением а не изменяет исходный массив - по сути конкатенирует массивы
	10. `arr.toString()` - преобразовывает массив в строку/отделает значения запятой без пробелов
	11. `arr.join()` - как мтеод toString но ток на стероидах, без аргументов работает также / с аргументом `("")` просто склеивает все знаечния юез разделителя - по сути мы и передали аргемнт разделителя кста? да- - короч мы передали разделитель - в данном случае пустую строку модешм такде передавть пробел `(" ")`  и тд
	12. `arr.includes(1)` или `arr.includes("audi")` - возвращает булевое значение. тру если эдемент найдн в массиве или фалсе если нет
	13. `arr.at(1)` - как и у строк позволяет образаться к элементам массива / переданные аушемнт по стуи является индексом + поддерживает отрицательные индексы
- полоезная штука (что делает?)
```js
const numbers = new Array(1, 2, 3, 4, 5);
const lastValue = numbers.pop(); // сохраняем значение удаленное из массива для будущизх операций с ней?
```
- статические меторды массива (т.к. находятся в рамках объекта Эррей)
	1. `Array.isArray(123)` - возвращает булеан значение, в зависимости от того явлется ли переданнный обхъект массивом или нет
	2. `const arr = Array.of(1, 2, "text")` - создает массив из переданных щначений, аргументы - элементы которые будут даовблены в массив
	3. `const arr = Array.from("1234")` - создает массив из перпеданной строки - разбирает строка на символы и возвращет массив -- `['1', '2', '3', '4']` помимо строки можно передать массив, мап, сет и тд


- вложенные массивы (типо двумерные треммерные или чо)
```js
const nestedArray = [1, 2, [3, 4], [5, 6], [7, [8, 9]]];
// обращение к 1му уровню
nestedArray[0] // 1

// обращение к 2му уровню
nestedArray[2][0] // 3

// обращение к 3му уровню - в релаьной разработке так лучше не делать
nestedArray[4][1][0] // 8
```
из такого массива мождно сделтаь так анзываемый плоский  массив / флет массив / квартира
`nestedArray.flat()` - чпреобразоввывает двумерный/трехмерный и тд массив в одномерный? принимает аргумент насоклько уровней вложенности типо копать - еслри передать 2 то будет преобразовывать 3х и 2х мерные массивы в однмоерный - блин сложно сформлуирвоать
если не занем скок вроней вложенности в массиве то аргументом можно передать Infinity -- .flat(Infinity)


- деструктуризация - не измняет исхгодный массив а просто копирует значения

```js
const names = ["Max", "Alex", "Stacy"];
// 1й способ - много кода
const nameMax = names[0]; 
const nameAlex = names[1];

// 2й способ - испольузется часто в разработке и более элегантный (деструктурируюбщее присваивание)
const [nameMax, nameAlex, dfsdsaf] = names; // nameMax = "Max", nameAlex="Alex", dfsdsaf = "Stacy" - элеенты присваиваются по порядку


//
const names = ["Kate"];
const [nameKate, nameOlga] = names; // nameKate = "Kate", nameOlga = undefined
const [nameKate, nameOlga = "Olga"] = names; // присваиваем значение по умолчанию / те есть ли элемента в массиве нету то присваиваем знаечние по умолчанию


// ПРИМЕР С РЕАКТОМ
// почти в кажом компоненте есть хуки, самый попялрный это useState
// оченб чатсо почти в кажом компоненте где етсь какоето состояние увидим такую запись
const [counter, setCounter] = useState(0);
// по стуи useState это функция НО функция с приставуой use принято считать хуком, useState возвращает кортеж
```



- **оператор** расширения (оч чатсо встречается на практике) spread

```js
// адача: есть два массива  с именами, мы хоти их объекенить в новый массив + добавить имена от себя (по сути могли бы использвоать конкат но этот способ лучше)
const names1 = ["Kate"];
const names2 = ["Max", "Alex", "Stacy"];

// это оператор состоит из 3х точек, по сути берет и расклдывает массив идущий после гнего на значения 
const allNames = [...names, "Nick", "Dima", ...names2];

// также работает и со строками (т.к стркоа по сути тож массив)
const strHi = "Hi";
console.log([...strHi]); // ['H', 'i']

// гнаходим наибольший и наимешьий элеемнт массива с помозщбю оператора спрэд
const numbers = [1, 24, 546, 12];
console.log(Math.max(...numbbers)); // по сути ралсожит весь массив на значения а max уже вернет маскимальное из них
```

- rest - по сути тоже мождно назвать опертоаром (остаточные параметры) - по сути работает противоположно, не раскалдывает массив а наоборрот собирает его из кусочков
```js
// пример
const sum (...numbers) => {
	const sum = 0;
	for(const value of numbers){
		sum += value;
	}
	return sum;
}

// можем передавтаь сколько гуодно аргументов 
sum(1, 2, 3, 4);
sum(1, 2, 3); 


// ================ еше пример
const names = ["Max", "Alex", "Stacy"];
const [firstName, ...restNames] = names; // присваиваем 1е значение перемнной firstName (firstName = "Max"), а все остальные знаечния массива присваиваем перменной restNames (по стуи оа будет явлться массиво с 2мя значениями)
```

- важно! остаточные парметры всегда должны рапсологаться в конце функции - как последний параметр/аргумент (если унее несколько аргументов)
	```js
	// correct
	const fn = (ar1, ar2, ...rest) => {}
	
	// ne pravilno
	const fn = (ar1, ...rest, ar2) => {}
	```

- spread & rest очень удобные штуки и используются повсеместно


---

# самые полезные методы ькоторый используются ОЧЕНЬ часто в релаьной разработке

- 
```js
const skills = ["html", "css", "scss", "js", "git", "ts", "react"];

// forEach - перебор массива
skills.forEach((value) => console.log(value)); // пробьегает по массиву, принимает колюэк функцию, value - элемент массива на каждой итерации после => идет то что мы делаем? 
skills.forEach((value, index) => console.log(value, index)); // колбэк фукнкци яомжет принимать 2 аргуемнты (1йф само значение второй его индекс)
skills.forEach((value, index, thisArray) => console.log(thisArray)); // также есть 3 й аругмент (названия аргументам можно давать любые), возвращает весь б массив на кажой итерации

// или иожем сделтаь так
const logValues = (value) => console.log(value); // создаем перемнну. с  колюэк функцией?
skills.forEach(logValues); // и передаем ее как ссылку (без скобок!)

// различие между 1м и 2м способом -- 1й мы передавали функцию сразу в скобках - как ананимную (обычно так и делают!!!) - но елси логика слишком большая можно использовать 2й вариант

// также есть 3й вариант
const logValues = (value) => console.log(value); 
function logValuesFr(value){ console.log(value); } // можем также доавблять сотальные аругемнты - index и thisArray
skills.forEach(logValuesFr);
```

- 
```js
const skills = ["html", "css", "scss", "js", "git", "ts", "react"];

skills.indexOf("js") // 3 (возвращает индекс первого вхождения эдлемента)
// также есть второй необязательный аргумент
skills.indexOf("js", 2) // -1 (возвращает индекс первого вхождения эдлемента) + поиски начинаются со второго индекса / -1 значит что элемент не найден
метод .lastIndexOf - такойцже как и indexOf но возвращает индекс послднего авхождения
```

- 
```js
const skills = ["html", "css", "scss", "js", "git", "ts", "react"];

// на кажой итерации мы в перменную value кладем значение и потмо это значение сранвивеам со строкой "js" / вернет true если эдлемент анйден /false - если не найден // ЕСЛИ ХОТЯБЫ ОДИН ЭЛЕМЕНТ ===
const isJs = skills.some((value) => value === "js")

// на кажой итерации мы в перменную value кладем значение и потмо это значение сранвивеам со строкой "js" / вернет true если все эдлементы подходят по условию /false - если нет // ЕСЛИ ВСЕ ЭЛЕМЕНТЫ === 
const isJs = skills.every((value) => value === "js")


const phones = [
	{id: 1, title: "samsungA50"},
	{id: 2, title: "iphone10"},
	{id: 3, title: "nokia3310"},
	{id: 4, title: "xiaomi"},
];

// на кажой итерации получаем элеемнт массива phone, и тк это объект то спрашиваем есть ли у данного объекта м (тип свойство или че) в кажом телефоне 
const everyHasTitles = phones.every((phone) => "title" in phone)
```





























